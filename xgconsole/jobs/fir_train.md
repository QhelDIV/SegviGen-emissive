title: Agentic training on fir: staging, env, multi-GPU launch
executor: fir-runner
track: research
status: ongoing
started: 2026-08-23 16:11
updated: 2026-08-25 19:50
slurm: 
link: 
page: none (training infrastructure job; results page comes with eval points)
upstreams: agentic_train,ddp_trainer
motivation: Owner: solar's GPU saturation pushes the 4-GPU window to Aug 30, so start the agentic training on fir instead, multi-GPU. Requires staging the 36k-shape dataset as tar shards, building the trellis2 env against the Alliance wheelhouse (or reusing the team's existing fir setup), and the DDP trainer just proven on solar.
log:
- 2026-08-23 16:11 [master] Job started.
- 2026-08-23 16:14 [fir-runner] Probed the Alliance cluster fir: a 4 GPU whole node with a 3 day limit starts immediately there, against a wait until Aug 30 for the same shape on our usual cluster. Fir has no 8 GPU nodes, so 4 GPUs on one node is the ceiling. Both candidate billing accounts schedule identically.
- 2026-08-23 17:21 [fir-runner] Measured the real cost of moving the training data: a single reader on our usual cluster gets 2.5 MB per second on this dataset, but 48 readers in parallel get 82, so the data is being packed into 48 large archives and should be ready to send within about an hour and a half. The network itself was never the problem.
- 2026-08-23 17:57 [fir-runner] Correction to my earlier note: the new cluster does not in fact start our job right away. The scheduler tool we used to estimate wait times reports an optimistic number that ignores the two thousand jobs already queued ahead of us. Which of the two billing accounts we use turns out to decide this, because one of them has plenty of unused allocation and the other is heavily used by the group; the difference puts us either near the front of the queue or in the bottom tenth. Raised with the lead as a decision for the owner.
- 2026-08-23 18:34 [fir-runner] Packed 352 of 450 GB of training data into archives on our usual cluster, with the copy to the new cluster chained to start as soon as packing finishes. Caught one real problem on the way: the validation archive first picked up was a half written leftover from a cancelled job, which a size check rejected; a plain copy would have trained the model against a quietly wrong validation set. Jobs on the new cluster are queued behind roughly a hundred thousand others, and past history for our account there points to a wait of one to five hours.
- 2026-08-23 19:54 [fir-runner] The new cluster now runs our training code correctly: a four GPU test loaded the existing model, trained real steps, scored a validation shape and saved checkpoints. The remaining obstacle is moving the data. Our own training job on the usual cluster has saturated its shared storage, which now serves under 2 MB per second, so the 456 GB of packed data would take over a day to copy. Pausing that job for about two hours would let the copy finish in under two, which is the decision now with the lead.
- 2026-08-23 20:06 [fir-runner] Measured real training speed on the new cluster with four GPUs: about 4.8 hours per pass over the data, so the full eight passes would take roughly 38 hours and fit comfortably in a three day slot. That is three to four times faster per pass than what we get on the usual cluster, where the same job is limited by shared storage rather than by the GPUs.
- 2026-08-23 20:35 [fir-runner] The slow copy is solved and no job needs pausing after all. The bottleneck was never the shared storage but the heavily loaded login machine we were reading through; this workstation mounts the same storage directly, and reading from it runs at 90 MB per second instead of under 2. All the data should be on the new cluster within about an hour and a half.
- 2026-08-23 21:49 [fir-runner] All 456 GB of training data is now on the new cluster, checked file by file against the original, and the eight pass training run is submitted and waiting for a four GPU machine. The job refuses to start unless it finds exactly the expected number of shapes, so a partial copy cannot quietly train on the wrong data.
- 2026-08-24 11:11 [fir-runner] Training is running on the new cluster and has finished two of eight passes over the data, each taking just under five hours, which matches the estimate. The score on the small validation sample rose from 0.05 after the first pass to 0.28 after the second. On present pace the run finishes around midday tomorrow, well inside its time limit. Billing was set to the account the owner chose; two small setup jobs run earlier went to the other account before that was settled.
- 2026-08-25 09:58 [fir-runner] Queued a follow-on training job that will pick up automatically the moment the current one finishes its eighth pass this afternoon, so the machine is never left idle. It starts from the current run's final weights and does eight more passes into a separate output folder, with a note in that folder explaining how its pass numbers line up with everything that came before. It checks that it can load all its code before doing anything expensive, which is the check that would have caught a failure another run hit earlier this week.
- 2026-08-25 10:05 [fir-runner] Found that the saved models on the new cluster were reachable by nobody but this session, which would have stalled the upcoming decision on whether to keep training. Set up an automatic copy into the shared location the evaluation reads from, so each saved model appears there within ten minutes of being written. The earlier ones are already copied and checked.
- 2026-08-25 11:07 [fir-runner] Updated the training code staged on the new cluster to the version that saves optimizer state, so every future continuation can pick up mid curve instead of paying a settling pass. The file matches the reference copy exactly, the currently running job is untouched because it took its own snapshot at the start, and the previous version stays recoverable from that snapshot if the check now queued finds a problem.
- 2026-08-25 19:50 [fir-runner] Cancelled the queued 3-day continuation. New fair-comparison run submitted: same 36k filtered data but starting from the original SegviGen checkpoint instead of the 72k-trained one, as a chain of six 12-hour jobs that resume from the latest saved checkpoint, which queues much faster than one 3-day job.
outcome: 
