title: Run graph of one training step with tensor shapes and argument types
executor: dataflow-scout
track: research
status: done
started: 2026-08-20 16:58
updated: 2026-08-23 19:16
slurm: 
link: 
page: train_rungraph
upstreams: train_dataflow
motivation: Owner ask after the data-flow page: a run graph of the ENTIRE training process, every call annotated with argument types and tensor shapes, to understand the process end to end. Captured from one real training step with a runtime tracer, rendered as an interactive graph page plus a static SVG.
log:
- 2026-08-20 16:58 [master] Job started.
- 2026-08-20 17:23 [rungraph-scout] Traced one complete training step in the real script and recorded every call it makes, with the shapes of everything passed in and out: 212 distinct functions, 394 call relations, 191,871 calls.
- 2026-08-20 18:02 [rungraph-scout] Built the run graph page: an interactive map of every call a training step makes, a one-screen diagram of the whole step with the tensor shapes on it, and the raw record as a data file. Two things it turned up that the code does not say anywhere: the model is handed twice as many voxels as the shape has and throws half the result away, and the backward runs all thirty transformer blocks a second time.
- 2026-08-20 18:04 [rungraph-scout] The run graph is built and published. Every call one training step makes is now recorded with the shapes of everything that crosses it, browsable on the page and kept as a data file. Two facts came out of it that were not written down anywhere: the model is given twice as many voxels as the shape actually has, because the thing to predict and the surface appearance are packed into the same tensor at the same coordinates, and half of what it computes is thrown away at the end; and the backward pass runs all thirty transformer blocks a second time, which is what the memory-saving trick costs. torchview was tried as well and is shown for what it cannot do: it does not follow the sparse tensor, so the chain through the blocks is missing from its picture.
- 2026-08-23 18:45 [master] Reopened for an owner follow-up: the collapsed process graph's blocks become clickable, opening an in-page code pane (source baked at the traced commit, function span highlighted; GitHub blob link secondary). Dispatched to rungraph-scout.
- 2026-08-23 19:01 [rungraph-scout] Made the step diagram clickable: any block whose name carries a dotted underline opens the code that ran, in a pane beside the page, with the function highlighted and scrolled to. The code is taken from the commit the trace ran on, not from today's files, which matters because the training script has grown by 390 lines since; a build check confirms all 210 recorded line numbers land on the right function.
- 2026-08-23 19:01 [rungraph-scout] The step diagram is now clickable: every block that stands for code opens that code in a pane next to the page, with the function highlighted and scrolled to, line numbers shown, and a link to the same lines on GitHub. The code shown is the code that actually ran, taken from the commit the trace was recorded on rather than from the current files, which matters because the training script has grown by 390 lines since; the build refuses to publish if any recorded line number fails to land on the function it names. The interactive graph's detail card offers the same view; opening a whole file lists the functions in it and jumps to any of them.
- 2026-08-23 19:16 [rungraph-scout] Fixed the spacing in the code pane: every line of code was followed by a blank one, which doubled the spacing and broke the highlight band into stripes. It now reads as ordinary continuous source, checked in both light and dark, and a test was added so the same fault cannot come back unnoticed.
outcome: The step diagram is now clickable: every block that stands for code opens that code in a pane next to the page, with the function highlighted and scrolled to, line numbers shown, and a link to the same lines on GitHub. The code shown is the code that actually ran, taken from the commit the trace was recorded on rather than from the current files, which matters because the training script has grown by 390 lines since; the build refuses to publish if any recorded line number fails to land on the function it names. The interactive graph's detail card offers the same view; opening a whole file lists the functions in it and jumps to any of them.
