title: Continue-train on the 36.6k agentic-QC split
executor: agentic-train
track: research
status: ongoing
started: 2026-08-23 12:55
updated: 2026-08-25 20:55
slurm: 
link: 
page: none (training job; the results page comes with its first evaluated checkpoint)
upstreams: filter_retrain,qc_filter_gallery
motivation: Owner: start training on the new agentic-QC-filtered dataset (36,652 survivors, the team's pinned _agentic split). Continue from the epoch-8 checkpoint with the proven recipe so training starts today; the multi-GPU trainer (ddp_trainer) takes over when it proves parity.
log:
- 2026-08-23 12:55 [master] Job started.
- 2026-08-23 13:00 [agentic-train] Agentic split verified: pinned train 36,255 shas (json indices == agentic_train_shas.txt, exact match); all 36,255 present in dataset_direct/train_72k, 0 missing. Zero overlap with val_72k (387), val72k_condok96_v3 (96), and the 381-shape eval5_samples_ep8 set. Note: the pin script docstring says 36,652 survivors but the split's own note records 36,255 (train 63,194 -> 36,255, dropped 26,939); the split file is the authority.
- 2026-08-23 13:01 [agentic-train] Built dataset_direct/train_72k_agentic: 36,255 symlinks (relative, ../train_72k/<sha>), all sources verified complete (cond.pth + input.vxz + output.vxz), 0 incomplete. Wrote outputs/emis_72kv2_cond_pw1b_agentic/{train,eval}_pw1b_agentic.sbatch in the cluster deploy. Smoke job 248418 submitted on 3dlg-hcvc-lab-debug (2-shape n_per_epoch, 1 epoch, save).
- 2026-08-23 13:02 [agentic-train] Walltime corrected 4d -> 5d: job 246931 (same recipe, 63,129 shapes) measured 23h08m and 22h49m per epoch (~1.31 s/shape), not the assumed 1.1. At 36,255 shapes that is ~13.2h/epoch and ~106h for 8 epochs, so a 4-day wall would truncate epoch 8. 5-00:00:00 is inside the long partition's 7-day cap.
- 2026-08-23 13:04 [agentic-train] Submitted: smoke 248418 (debug, 2 shapes), real run 248419 (3dlg-hcvc-lab-long, gpu:l40s:1, 8 cpus, 64G, 5-day wall) chained with --dependency=afterok:248418 so it launches automatically on a clean smoke and is cancelled on a failed one. Cluster is fully GPU-saturated right now (all three l40s nodes 4/4 allocated, a40/a100 also full), so both are pending; the first l40s frees when job 246931 hits its 4-day wall on cs-venus-16.
- 2026-08-23 13:06 [agentic-train] Requeued on team-lead's instruction with cs-venus-17 dropped from the exclude list (now 05,09,15,19: the standing project set). Reason recorded in all three sbatch headers: 17 appears in no standing list and with no documented reason, and since l40s exists only on cs-venus-15/16/17, excluding both 15 and 17 left one eligible node. Cancelled 248418/248419; new smoke 248420 (PD, Priority), new real run 248421 (PD, Dependency, afterok:248420). If a job lands on 17 and fails node-shaped rather than recipe-shaped, 17 goes back and the evidence gets logged here.
- 2026-08-23 14:32 [agentic-train] Smoke 248420 FAILED at 46s on cs-venus-17: EmisDataset raised 'cond.pth missing' for train_72k_agentic/01ff1d4f7aec4b379cb605047d72b097. That file exists (8,431,070 bytes, dated Aug 7) and is visible from the workstation, the login node, and from cs-venus-17 itself (verified by srun pinned to that node: os.path.exists True for cond.pth and all three core slat files). So it is not persistent node blindness. Second finding: my pre-flight checked the wrong file set. I verified cond.pth/input.vxz/output.vxz, but EmisDataset gates on shape_slat.pth/input_tex_slat.pth/output_tex_slat.pth (silently skipped if absent) and then cond.pth (hard error); input.vxz and output.vxz are never read. Job 248438 is now re-counting with the real predicate over all 36,255 agentic dirs plus train_72k_nonzero_nocopy as a control. 248421 left DependencyNeverSatisfied; no resubmit until the count is in.
- 2026-08-23 14:37 [agentic-train] DIAGNOSIS RESOLVED, not a data gap and not node-sticky. Answer to the NFS-node hypothesis: cs-venus-17 and the login node BOTH report the failing path present, so re-excluding 17 would record a wrong reason; 17 stays in the pool. The count job 248438 (cs-venus-07) flagged 11 agentic shapes as missing cond.pth and 18 in train_72k_nonzero_nocopy. All are false. Control that settles it: the filtered run 246931 has trained on _nonzero_nocopy with --cond real for over three days, which is impossible if any of its 18 were really missing, and the agentic 11 are a strict subset of that 18. Each of the 11 reads fine from the workstation, the login node, and a compute node when asked again. Mechanism: transient NFS lookup misses during a cold scan of a large fresh tree, cached as negative dentries on the scanning node (same node reported the identical set missing in two back-to-back jobs; a later check elsewhere saw them present). EmisDataset turns one such miss into a hard crash at startup. REAL finding, separate: 29 of the 36,255 genuinely lack a core slat file (re-checked individually, 29 of 29 confirmed), which my first build predicate missed because I checked cond.pth/input.vxz/output.vxz instead of the three slat files. Those 29 are exactly the ones the older _nonzero_nocopy farm had already excluded, explaining its 63,129-vs-63,194 gap. Farm rebuilt to 36,226 links. Both sbatch files now run preflight_split.py first: EmisDataset's exact predicate, a negative retried and confirmed before it is believed, cache warmed for the trainer's scan, nonzero exit on a real gap. Trainer commit at launch (b1742dc, DDP) recorded in the header. New chain: smoke 248441, real run 248442 (afterok).
- 2026-08-23 15:33 [agentic-train] 4-GPU takeover job submitted: 248452 (emis_pw1b_agentic_ddp4, 3dlg-hcvc-lab-long, gpu:l40s:4, 16 cpus, 256G, 2-18:00:00). Recipe unchanged, no --grad_accum (conservative 2.23x at effective batch 4), lr stays 1e-5, preflight kept. Two takeover steps run at job start so they are atomic with it: resolve --init_ckpt to the newest COMPLETE per-epoch checkpoint the 1-GPU run has written (size-checked against the epoch-8 reference so a half-written file is skipped), else the epoch-8 warm start; set --epochs to the remainder so the two jobs total 8; then scancel the 1-GPU sibling BY NAME (emis_pw1b_agentic) in any state, which survives a resubmit and cannot match this job. Resolution logic tested offline against four cases (no checkpoints, three complete, one truncated mid-write, all eight done -> clean no-op exit). squeue name matching verified exact against the live queue. DEVIATION, and it is what README_DDP.md requires: --out_dir is a NEW directory (emis_72kv2_cond_pw1b_agentic_ddp4) because the trainer numbers epochs from 1 within a run, so writing into the 1-GPU dir would overwrite the very checkpoints the takeover resumes from. Total agentic epochs = epochs in the 1-GPU dir + epochs in the ddp4 dir = 8; a TAKEN_OVER_BY_DDP4.txt marker is written into the 1-GPU dir at handoff. Chain now: smoke 248441 -> 1-GPU 248442 (afterok), with 248452 taking over whenever a 4-GPU slot frees.
- 2026-08-23 15:40 [agentic-train] Trainer comparability CLOSED. ddp-engineer's probe (job 248451) shows step 1 of the pre-DDP trainer and b1742dc is BITWISE equal with WORLD_SIZE unset, same seed, emis_oversample: 0.028364012017846107. Identical bits from identical starting weights mean the epoch draw, shape order, flow noise and timestep are unchanged. Past step 1 the sequences drift, but the old trainer drifts from ITSELF by the same amount (2.495e-05 old-vs-old vs 2.633e-05 old-vs-new over steps 2-20, across-change gap larger on 12 of 19 steps), because flash_attn and the sparse ops reduce with atomics. General lesson recorded in the sbatch header: this model is not bitwise reproducible past step 1 for any version, so any future behaviour-change check on this codebase needs a same-file control or it reports a false positive. So the agentic run stays comparable to 246931. Two artifact-format changes noted, neither touching training: log.json gains epoch_sec/shapes_per_s/world_size/grad_accum, and the epoch console line gains a timing suffix (my monitor regex verified still matching). The one real behaviour change in b1742dc, a cosine horizon using a clamped n_per_epoch, cannot reach this run (lr_schedule const, no n_per_epoch). Both sbatch headers updated with the evidence; SLURM copies scripts at submit time so the queued jobs run the earlier copies, which differ only in comments. Also relayed to ddp-engineer that their excluded_v2 '41 of 286 missing cond.pth' is almost certainly the same false-negative artifact.
- 2026-08-23 15:50 [agentic-train] Smoke 248441 RUNNING on cs-venus-17, gate passed: PREFLIGHT_OK admitted=36226, exactly the farm size. The gate also caught the phenomenon live rather than theoretically: 20-plus cond.pth lookups failed and recovered after a single retry. Training proceeding in that job. TWO CORRECTIONS from ddp-engineer, both accepted. (1) Their excluded_v2 41 is REAL, not the false-negative artifact I attributed it to; I was wrong to generalise my control to their split. Their discriminator is better than mine and needs no control job: a cached negative dentry suppresses a LOOKUP but does not remove the name from a readdir, so a file missing from ls of its parent is genuinely missing, while a file that shows in ls but fails os.path.exists is the cache phenomenon. My 11 show in ls; their 41 do not. Both conclusions stand under the sharper test. (2) A labelling bug in my preflight_split.py: a give-up returned tries=4, which is truthy, so failures were appended to the recovered list as well as to hard_fail. The exit code was always right (it keys off hard_fail) so the gate never mislabelled a launch, but transient_recovered=N could read as N handled misses when it was N failures. Rewritten around the readdir discriminator, classifying every negative as ABSENT (real gap, exit nonzero), RECOVERED (printed, never swallowed) or LOOKUP_STUCK (in the listing but exists() keeps failing, exit nonzero). Verified both directions: on excluded_v2 it reports absent=41 recovered=0 and exits 1, reproducing ddp-engineer's number exactly; on val72k_condok96_v3 it passes clean. Full re-verification on train_72k_agentic in progress. The fix reaches the queued jobs because the .py is read at runtime, unlike the sbatch which SLURM copies at submit.
- 2026-08-23 15:51 [agentic-train] SMOKE_OK. Job 248441 COMPLETED on cs-venus-17 in 11m04s: gate passed (PREFLIGHT_OK admitted=36226), EmisDataset loaded all 36,226 samples, one training step ran (flow_loss 0.35738), val_quick ran and a checkpoint saved (best.ckpt -> epoch_0001.ckpt), peak GPU 18.87 GiB of 46 GiB. The dependency has released: 248442 is now PD on Priority with a SLURM start estimate of 2026-08-23 23:51. 248452 (4-GPU takeover) also PD on Priority, no estimate yet. Two measurements worth keeping: the trainer's own scan of 36,226 dirs took 8m52s, and the preflight recovered 20-plus cond.pth lookups that failed on the first try. Per ddp-engineer's refinement, the labelling bug only made entries with tries==4 ambiguous, and every RECOVERED line in this smoke reads 'after 1 retries' with none at 4, so they are all genuine transient misses caught and cleared. Both effects in this incident were real and split-specific: transient negative-dentry misses on ours, a genuine build-coverage gap on excluded_v2.
- 2026-08-23 15:59 [agentic-train] Trainer under this run is now 76eb677, NOT b1742dc. Verified independently: deploy and canonical checkout both md5 7355eff1e7c0ca3d122961adc0d81c0d, working tree clean, and ddp-engineer's 19-check suite passes here (19/19). Mechanism worth recording because it bit us twice today: SLURM copies the SBATCH SCRIPT at submit, but the trainer is an ordinary .py read from the shared deploy at job START, so a queued job runs whatever is committed when it begins, not when it was submitted. My headers said b1742dc; both are corrected, though the copy SLURM already took for 248442 still carries the old claim, so the board is the authority on that point. Read the change rather than trusting the summary: probe_exists returns on its first line when the file is present, so the happy path IS os.path.exists at zero cost, and admission semantics are unchanged. ONE COMPARABILITY NOTE: the fourth probe site is meta.json, which neither my preflight nor I had flagged. Under the old code a transient miss there silently set emissive_frac to 0.0 and mis-weighted that sample under --emis_oversample, which this recipe uses. So on a transient miss this run now weights a sample correctly where 246931 would have weighted it wrong: a correctness fix, not drift, but the two runs are not bit-identical in that rare case and the difference favours ours. My rewritten preflight also re-verified clean on the full split: admitted=36226, absent=0, lookup_stuck=0. Preflight stays in place this run per the lead.
- 2026-08-23 16:05 [agentic-train] All four items of the lead's decision implemented. (1) Freeze: lead coordinates, nothing for me. (2) Queue slot kept, no resubmit. (3) Start-time capture: emissive/slurm/capture_trainer_version.sh writes TRAINER_AT_START.txt into the run's output dir with the deployed trainer's md5, git blob, and the commit that blob resolves to in the canonical checkout. It does NOT assume the deploy is a git checkout or that it matches canonical; it hashes what is there and says plainly when the blob matches no commit. Dry-run resolved 76eb677 correctly even though canonical HEAD had already moved to 27a3ab0, so it pins the trainer rather than HEAD. Monitor re-armed to fire it on RUN_START for both 248442 and 248452. (4) Self-pinning convention written to emissive/slurm/README_RUN_PINNING.md, cross-referenced from README_LOGS.md, and adopted in both sbatch files on disk. IMPORTANT CORRECTION I found while implementing it: pinning the entry point alone does NOT work. train_emissive.py finds the repo root by walking UP from its own file to inference_full.py and puts that root on sys.path AHEAD of the script's directory, so a lone copy still imports inference_full, eval_emissive and trellis2 from the live deploy. My first version had exactly that hole and the doc claimed the trainer was self-contained, which is false. The convention now copies the whole import surface (inference_full.py + trellis2/ + emissive/{train,eval}) into OUT_DIR/code so the walk-up lands on the pin; verified empirically that it resolves to the pin, 1.9MB and a couple of seconds. Documented what is still NOT pinned: the conda environment. The queued 248442/248452 run the pre-retrofit copies SLURM already took, which is what item 3 covers; a resubmit of either now self-pins.
- 2026-08-23 16:06 [agentic-train] Trainer freeze verified independently: md5 still 7355eff1e7c0ca3d122961adc0d81c0d on deploy and canonical, and git log 76eb677..HEAD on emissive/train/train_emissive.py is empty, so nothing has touched it. ddp-engineer's 27a3ab0 is one file changed, the test only, confirmed by --stat. Their docs fix works: the probe suite is now 19/19 under plain python3 as well as under a torch-bearing interpreter, and the stubbed run announces its reduced coverage. I checked the negative case too, that a torch run does NOT claim stubbing, since a notice that always fires would be as useless as one that never does; it does not. Everything is now waiting on the queue: 248442 PD on Priority, SLURM estimate 2026-08-23 23:51; 248452 PD on Priority, no estimate, waiting on a 4-GPU slot. Monitor armed on both for the trainer-version capture at RUN_START, epoch lines, takeover markers and failure signatures.
- 2026-08-23 16:07 [master] Owner directed trying cs-venus-15 for the multi-GPU run. Board record shows its standing exclusion has no direct evidence (the unexplained 2026-08-11 hang was on venus-09; 15 was excluded by association). Dropped 15 from the ExcNodeList of pending 248442 and 248452 via scontrol update, no queue position lost; eligible l40s pool is now 15/16/17. If a run later fails node-shaped on 15, re-exclude with the evidence logged.
- 2026-08-23 16:09 [agentic-train] cs-venus-15 un-exclusion verified on both jobs: scontrol now reports ExcNodeList=cs-venus-05,cs-venus-09,cs-venus-19 for 248442 and 248452, so the eligible l40s pool is 15/16/17. Closed the gap that opens: the on-disk sbatch files still carried the old four-node list, so any resubmit would have silently reverted the owner's direction. All five agentic sbatch files (train, eval, ddp4, smoke, splitcheck) now read exclude=05,09,19 and carry a revised rationale explaining why each node is on or off the list, plus a note that 248442/248452 were changed SLURM-side because SLURM holds their original scripts. TIMING FINDING, needs a decision. Un-excluding 15 did not move 248442 (still est 2026-08-23 23:51) but 248452 now has its first estimate: 2026-08-30 08:37. At 13.2h/epoch from a 23:51 start, the single-GPU run reaches epoch 8 around Fri Aug 28 09:36, roughly two days BEFORE a 4-GPU slot is expected. So the takeover as queued will most likely find the run already complete and hit TAKEOVER_NOOP, exiting 0 without training, which is the designed behaviour but means the 4-GPU path delivers nothing on this schedule. Recommend keeping it queued anyway as insurance (it costs a brief slot, self-cancels correctly, and covers the case where 248442 is preempted or falls behind), but flagging that the expected outcome is now a no-op rather than a 2.23x speedup.
- 2026-08-23 16:11 [agentic-train] Eval cadence armed. Standing authority from the lead: score every EVEN epoch as its checkpoint lands, epoch 2 first, raw weights, eval_pw1b_agentic.sbatch, no per-eval go-ahead. Implemented as an automatic watcher folded into the run monitor: for epochs 2/4/6/8 it waits for epoch_000N.ckpt to reach the reference size 2620070846 (so a checkpoint caught mid-write is skipped rather than evaluated truncated), submits the eval once, records the job id, then parses and reports the number when eval/val72kok_raw_real_agentic_epN.json appears. Tested against fabricated inputs before arming: submits once and reports once, silent on a repeat tick, correctly waits on a truncated checkpoint and submits when it completes, and raises EVAL_ATTENTION if ddp4 checkpoints appear since the takeover restarts epoch numbering at 1 and cannot be mapped to continued-epoch counts by filename alone. Comparison baseline confirmed from the filtered run's own json rather than from memory: filtered ep2 best_iou 0.15248 (n=96), nonzero 0.12731 (n=85), 5 draws. Filtered ep4 checkpoint now exists (written today 13:29) so its eval 248430 has a valid target; that job is still PENDING. Reference for both remains the epoch-8 baseline 0.198 on the same frozen 96-shape subset. Job 246931 is at 3-23:12 of its 4-day wall and will hit it within the hour, which is expected and not a fault.
- 2026-08-23 17:26 [agentic-train] 248442 RUNNING on cs-venus-17, started 17:04:50, about seven hours ahead of the 23:51 estimate because un-excluding 15 freed the pool. Trainer capture fired automatically at RUN_START and recorded 76eb677 into TRAINER_AT_START.txt, resolution byte-identical to that commit. Both scans clean and, notably, 0 recoveries this time: PREFLIGHT_OK admitted=36226 transient_recovered=0, then the trainer's own scan reported 36226 admitted, 0 transient misses, 0 stuck, 0 absent. The preflight warming the cache before the trainer scanned is exactly the intended behaviour. REVISED SCHEDULE at 13.2h/epoch: ep1 ~Mon Aug 24 06:25, ep2 ~Mon 19:37 (eval auto-submits), ep3 ~Tue 08:49, ep4 ~Tue 22:01 (eval), ep5 ~Wed 11:13, ep6 ~Thu 00:25 (eval), ep7 ~Thu 13:37, ep8 ~Fri Aug 28 02:49 (eval). Run finishes ~2.2 days before the 4-GPU slot estimate of Aug 30 08:37, so the takeover no-op expectation is reinforced. Filtered ep4 verified from its json rather than from the message: IoU@0.5 all 0.19865, nonzero 0.21965, n=96/85, 5 draws. Both filtered evals are essentially threshold-flat (sensitivity 5e-4 to 7e-4), so IoU@0.5 and best_iou agree to the third decimal and the earlier quantity ambiguity is moot; standardising on IoU@0.5 as the lead does.
- 2026-08-23 17:28 [agentic-train] Draw-count protocol implemented per the lead: ep2/ep4 at 5 draws (symmetry with the filtered arm's existing points), ep6/ep8 at 10 (the points that would carry an arm-difference claim). eval_pw1b_agentic.sbatch now takes an optional third argument $3=draws defaulting to 5, with the rationale and the 0.92-se arithmetic in its header; the watcher passes 5 or 10 per epoch. Draw count is baked into the output tag (val72kok_raw_real_agentic_epN_dM) as well as the json, so a mixed-draw column cannot be compared silently even by someone reading only filenames. Reporting standardised on IoU@0.5 with best_iou kept alongside so a threshold shift would show rather than hide, and every reported point now carries draws, draw_std, the standard error and a 95% band. Parser verified against the real filtered ep4 json: reproduces 0.1987 all / 0.2197 nonzero / se 0.0196 exactly. Walltime checked rather than assumed: a 5-draw eval measured 19-21 min (247647, 248430), so 10 draws lands near 40 min, well inside the 3h wall, no change needed. Table cells will be written as value (draws=N) from here on.
- 2026-08-23 18:02 [agentic-train] INCIDENT, my fault, 53 minutes of training lost and nothing was running for ~25 min. Sequence: ddp2 job 248521 got its allocation on cs-venus-17 at 17:58, cancelled the healthy single-GPU run 248442 as designed, then died 80 seconds later on ModuleNotFoundError: No module named 'data_toolkit'. ROOT CAUSE is my self-pinning convention. The pin copied inference_full.py + trellis2/ + emissive/{train,eval} and made the trainer's repo-root walk-up land on the copy, which redirected sys.path to the pin; inference_full.py imports data_toolkit.bpy_render at module scope and data_toolkit was not copied. I had verified the walk-up resolved to the pin and that the three copied surfaces existed, but I never RAN the pinned copy, so the missing fourth surface went unseen. My '1.9 MB and a couple of seconds' claim was also wrong: data_toolkit alone is 93 MB, and the reachable import set includes o_voxel and trellis whose provenance I never established. TWO FIXES. (1) Self-pinning REVERTED in all three sbatch files; they run the deploy trainer directly, which is the configuration 248442 ran successfully for 53 min. TRAINER_AT_START.txt still records which bytes ran, which was always the fallback and is now the whole mechanism. (2) New IMPORT GUARD in both takeover scripts, placed BEFORE the sibling cancel: the job runs the trainer with --help, which executes every module-level import in its exact interpreter, path and cwd, then exits 0. With set -e an import fault aborts the takeover and the incumbent keeps running. A takeover must never terminate a working run on the strength of merely having been allocated a GPU. RESUBMITTED: single-GPU 248525, ddp2 248527, ddp4 248528 (all PD). Also cancelled a duplicate ddp2 248526 that a double submission created. venus-17's free pair was gone by the time the fix was ready (back to 3 of 4 allocated), so the --nodelist pin was dropped from ddp2 on resubmission since the premise it encoded no longer held.
- 2026-08-23 18:04 [agentic-train] RECOVERED and training. ddp2 248527 took two l40s on cs-venus-17 at 18:02:20 and took over in the correct order this time: IMPORT_GUARD_START then IMPORT_GUARD_OK after 11s, and only then TAKEOVER_CANCELLED on the single-GPU sibling 248525. The guard caught nothing because there was nothing to catch, but it ran on the exact fault class that caused the outage four minutes earlier, which is the point. Resolution correct: 0 epochs done, init from the epoch-8 warm start, 8 remaining. Now training with world_size=2, grad_accum=1, effective batch 2 shapes/step. At ~9.2h/epoch that puts ep8 around Wed Aug 26 evening, better than the single-GPU Friday. Trainer capture recorded 76eb677. 248528 (ddp4) still queued as insurance and will chain correctly through ddp2 if a full node appears. README_RUN_PINNING.md rewritten honestly: it now documents that copying code into the run directory was tried, failed in production, and is NOT in use, with both lessons stated plainly (verifying the mechanism is not verifying the result; the cost estimate was wrong by a factor of fifty because it counted only what had been copied). It also generalises the design rule the incident exposed: any job that terminates another job must first prove it can do the work it is taking over, because an allocation is not a capability. README_LOGS.md cross-reference corrected to match.
- 2026-08-23 18:06 [agentic-train] FULL SEQUENCE OF THE 18:00 RACE, from sacct rather than from memory. 248442 (1-GPU, mine) cancelled 17:58 by 248521's handoff. 248521 (ddp2, mine) FAILED 80s later on data_toolkit: the outage, my pin bug. 248523 (ddp4, mine) cancelled by me when I resubmitted it as 248528. 248524 (ddp2, team lead's patched variant) started 18:01:19 and was cancelled 29s later by the lead. 248525 (1-GPU, mine) started 18:02:19 and was cancelled 12s later by 248527's handoff, correctly and after its import guard passed. 248526 (ddp2, submitted 18:01:49 by the LEAD from my reverted file) never started: I cancelled it at 18:02 believing it was a double submission of my own, since it appeared in squeue seconds before my 248527 with an identical name. That was my error, not a duplicate. 248527 (ddp2, mine, submitted 18:01:55) is the LIVE RUN, on cs-venus-17 since 18:02:19. The lead's handback message names 248526 as live; it is not, it is cancelled. No harm done: 248526 and 248527 came from the same reverted file, both 2-GPU and unpinned, and 248527 additionally lacks the --nodelist pin, so the surviving job is the better of the two. Root cause of the confusion is two writers submitting from one file inside the same minute; single-writer is back in force and I own the file and the job. LIVE STATE: 248527 training, world_size=2, 18113 optimizer steps/epoch, warm start from epoch_0008, 8 epochs remaining. 248528 (ddp4) queued; verified its held script already carries SIBLING_NAMES covering both emis_pw1b_agentic and emis_pw1b_agentic_ddp2 plus the import guard, so the sibling gap the lead flagged is closed in-script, which is stronger than a monitor-side cancel because it is atomic with the takeover. EVAL WATCHER REPOINTED, this was a live defect: it was watching the 1-GPU directory, which will never receive another checkpoint, so no eval would ever have fired. It now finds the directory holding the head of the campaign and maps filename epochs to CONTINUED epochs through the handoff markers, tested for both the current single hop and a future ddp2-to-ddp4 second hop. THREE-GPU QUESTION: declined, no churn. venus-17 is at 3 of 4 allocated of which ours is 2, so only one GPU is free; widening to 3 is not available, and the lead reports preemption is dead because mars-lab-long shares our PriorityTier of 80.
- 2026-08-24 11:12 [agentic-train] ep2 eval was submitted on time and FAILED on my bug; resubmitted and now correct. The watcher did fire at 10:01, one minute after epoch_0002.ckpt landed, as job 248686. That job died in 84s with FileNotFoundError on the checkpoint. Cause: the watcher runs on the workstation where the share is /cs/3dlg-jupiter-project/..., while compute nodes mount the SAME share as /3dlg-jupiter-project/... with no /cs prefix, so the path it handed to sbatch could not exist on the node. This is a documented project fact (compute nodes mount /project only, never /cs) that I failed to apply when I repointed the watcher at the ddp2 directory last night. Fixed by translating the path at submit; resubmitted as 248718. Also worth recording so nobody hunts in the wrong place: eval_pw1b_agentic.sbatch writes its json and log into outputs/emis_72kv2_cond_pw1b_agentic/eval/, the 1-GPU directory, NOT the ddp2 run directory. That is deliberate, one central place for the comparison table across every arm and width, but it means an absent eval/ dir under ddp2 is expected rather than evidence of a missing eval. TRAINING PACE, from the run's own lines: epoch 1 flow_loss 0.31487 at 1.257 shapes/s (28820s), epoch 2 flow_loss 0.30940 at 1.271 shapes/s (28492s), so ~7.95h/epoch against the 9.2h I projected, and 8 epochs land Wed Aug 26 around 06:00 rather than the evening. WATCH ITEM, not yet a conclusion: the 8-sample quick-val fell from 0.0878 (nonzero 0.1004) at epoch 1 to 0.0206 (nonzero 0.0235) at epoch 2. That is 8 shapes and enormously noisy, and the filtered arm showed the same shape of early dip before recovering by its epoch 4, but it is the number to watch. The real 96-shape eval at 5 draws is what decides it.
- 2026-08-24 20:19 [agentic-train] EVAL CADENCE EXTENDED TO THE FIR ARM, and the solar ep2 finally unblocked. Solar ep2 (249075) is running past the point its two predecessors died. Second failure explained: 248718 died 14 min in, mid-generation, on 'cond.pth missing' for val72k_condok96_v3/2b6a97c489f740869715f997645b50af, a file that IS in its parent listing, so by the readdir discriminator it is the transient lookup miss again. The cause is that eval_emissive.py line 116 still uses a bare os.path.exists: ddp-engineer's probe_exists fix landed in train_emissive.py's EmisDataset only, so the eval path has no confirm-before-believing. Worse there than in training, because EmisDataset takes its misses during the up-front scan while the eval opens cond.pth per sample during generation, so a miss lands after real GPU work. Unblocked without touching shared code: eval_pw1b_agentic.sbatch now warms val72k_condok96_v3 with preflight_split.py before scoring. Raised the real fix with ddp-engineer, including two unguarded meta.json reads at lines 91 and 94 that would silently default emissive_frac to 0.0 and mis-stratify rather than crash. FIR ARM: epoch_0002 and epoch_0004 copied from fir to outputs/emis_72kv2_cond_pw1b_agentic/fir_arm/ over the ControlMaster channel at ~105 MB/s, and verified by MD5 against fir, not just by size, because a manual copy and a watcher copy raced on ep4 (c23988884c7ed22fe875e06144ddc160 and 526301797d3351acc1c32723db902189, both matching). Evals submitted: fir ep2 job 249080, fir ep4 job 249082, 5 draws each, tags val72kok_raw_real_agentic_fir_ep{2,4}_d5. A per-epoch mkdir lock now prevents concurrent copies; the race was harmless only because rsync renames atomically, which is luck about rsync rather than correctness in my script. Going forward the fir watcher polls the fir run dir each monitor tick for new even-epoch checkpoints, copies, and submits at the standing draw counts (ep6/ep8 at 10). fir epoch N maps to continued epoch N: that arm started from the same epoch-8 warm start with no prior continuation, so there is no handoff offset. It also reports FIR_CHANNEL_DOWN rather than failing silently if the ControlMaster socket expires, since only the user can restore it (MFA).
- 2026-08-24 20:21 [agentic-train] AXIS DECISION RECORDED and now enforced mechanically rather than by my discipline. Team lead 2026-08-24: the PRIMARY read is each arm against its own shared warm-start reference (0.198) and its bucket-profile shift, at equal continued EPOCHS (data seen). Cross-arm deltas at equal epochs are SECONDARY and must always be printed with both caveats: effective batch 4 (fir) / 2 (solar ddp2) / 1 (filtered), hence a quarter and a half the optimizer steps at equal epochs; and the meta.json weighting fix from 76eb677 is present in the agentic arms but not in the filtered run. Rationale, which I agree with: the campaign asks a data-policy question (agentic vs nocopy) and every current pairing confounds data policy with batch size, so no axis choice can make fine cross-arm deltas carry conclusions; what can carry them is whether each arm clears its own reference and how its behaviour profile moves. No updates-equalised axis is to be presented unless asked, and if asked the numbers shown are optimizer STEPS, not rescaled epochs. Both eval watchers now print the reference comparison automatically on every result: delta from 0.198, that delta in standard errors, and one of three verdicts (clears / below / INDISTINGUISHABLE from the reference). This removes the chance that a number gets reported without its uncertainty and read as a movement. Applying it retroactively to the filtered arm's existing points is instructive: filtered ep4 is +0.0007 against the reference, which is 0.03 se, INDISTINGUISHABLE. So the 'fully recovered to 0.198' reading was not merely overstated, the point is statistically indistinguishable from the reference AND from its own ep2, which is a weaker claim than either framing implied. STATUS: three evals running and all cleared the warm step (249075 solar ep2 on venus-15, 249080 fir ep2 on venus-16, 249082 fir ep4 on venus-17); venus-15 carrying work confirms the un-exclusion was worth making.
- 2026-08-24 20:35 [agentic-train] FIRST AGENTIC NUMBER. Solar ddp2, continued epoch 2, 5 draws, n=96 (full subset, no shapes dropped): IoU@0.5 all 0.1444, nonzero 0.1278 (n=85), draw_std 0.1009, se 0.0451. PRIMARY READ per the axis decision: vs the 0.198 reference that is -0.0536, which is -1.19 se, INDISTINGUISHABLE from the reference. The 95% band is 0.056 to 0.233 and contains the reference comfortably. Threshold-flat as usual (0.1444 to 0.1463 across 0.2-0.5). SECONDARY, with both caveats attached: against the filtered arm's ep2 (0.1517) the cross-arm delta is -0.0073 at se 0.0651, which is -0.11 se, indistinguishable; and the two are not interchangeable anyway, since ddp2 ran effective batch 2 against filtered's batch 1, so half the optimizer steps at equal epochs, and the agentic arm carries the 76eb677 meta.json weighting fix that the filtered run does not. So at epoch 2 both arms sit inside noise of their shared warm start and of each other. That is the expected shape at 5 draws given the spreads: with draw_std near 0.10, a 5-draw point cannot resolve anything smaller than about 0.09, and none of the differences in play are that large. The ep6 and ep8 points at 10 draws are where resolution arrives. SAMPLE-COUNT GUARD proved worth adding immediately: this eval ran on the pre-e097a70 code, so it was exposed to the silent-drop at eval_emissive.py line 191, where a transient miss on output_tex_slat.pth dropped a shape without a log line and changed the IoU denominator. n came back 96, so this point is clean and comparable. Both watchers now assert n==96 on every result and flag a short count as not comparable. Fixing that guard exposed two bugs of my own worth recording: a separate print() for the warning was swallowed by the tail -1 the caller pipes through (found only because I tested it with a synthetic short-n result rather than trusting it), and the comment I wrote to explain that contained backticks, which inside the double-quoted python -c argument became command substitution and hung the watcher tick. Both fixed, both watchers now backtick-free and timed to complete in seconds.
- 2026-08-25 09:55 [agentic-train] HOP TO 4-GPU DONE, after one failure that was mine. Job 248528 took the node at 09:52:56 and FAILED five seconds later with no output. Cause: in my two-hop resolver the helper newest_in ended with a bare test-and-print, so the test was the function's last command and an EMPTY directory made it return 1; under set -e the enclosing command substitution then took the whole script down. The 1-GPU directory IS empty in the normal case, because ddp2 took over before the single-GPU run ever wrote an epoch, so this fired on its first real use. My six-scenario test missed it because the harness ran the block under 'set -uo pipefail' WITHOUT -e: I tested the logic and not the shell it actually runs in, which is the same shape of mistake as the pin (verifying the mechanism, not the artifact). Fixed to an explicit if plus return 0, retested under the real set -euo pipefail on both the live case and the both-dirs-empty case. Resubmitted as 249196, RUNNING on cs-venus-17 since 09:54:08. Resume verified end to end: TAKEOVER_CHAIN reports 0 before the ddp2 handoff plus 5 inside it, resolved to ddp2/epoch_0005.ckpt with 3 epochs remaining, import guard passed in 22s, handoff marker written recording 5 of 8. Trainer at start captured as e097a70, so this run carries the fixed eval-path probe. Eval mapping re-verified against the real markers: ddp4 epoch 1 maps to continued epoch 6 at 10 draws. Monitor repointed to 249196 as the campaign head. Total idle window was about 80 seconds of a node the owner wanted busy.
- 2026-08-25 09:58 [agentic-train] MY ERROR BARS WERE WRONG BY ABOUT 10x AND EVERY 'INDISTINGUISHABLE' VERDICT I REPORTED IS RETRACTED. eval_emissive.py line 237: draw_std is the MEAN OVER SHAPES of each shape's own across-draw std (per-shape iou_std_by_thr = np.std over that shape's draws, then averaged over shapes). It is a within-shape variability measure, NOT the standard error of the set-level mean. I read it as the latter and computed se = draw_std/sqrt(draws). The correct set-mean standard error from draw noise is draw_std/sqrt(n_shapes*draws), which with n=96 is about ten times smaller. Corrected, using IoU@0.5 against the 0.198 reference: solar ep2 0.1444 is -11.6 se (I said -1.19); solar ep4 0.1516 is -11.8 se (I said -1.21); fir ep2 0.1155 is -18.9 se; fir ep4 0.1191 is -16.9 se; fir ep6 0.1709 is -6.3 se; filtered ep2 0.1517 is -9.7 se (I said -0.99); filtered ep4 0.1987 is +0.3 se, still indistinguishable. So the true picture inverts almost everything I reported: every agentic point measured so far sits FAR BELOW the warm-start reference, not within noise of it, and the filtered arm's ep2 was a real dip after all while its ep4 genuinely is at the reference. My correction of the 'fully recovered' framing was itself wrong in the ep2 direction: there WAS a dip and there WAS a recovery. Two caveats on the corrected figure, both stated rather than buried: draw_std is the mean of per-shape stds where correct pooling uses the RMS, and mean(s) <= rms(s), so the corrected se is if anything a slight under-estimate; and this accounts for draw noise only, so a standalone confidence interval on one arm's absolute IoU would additionally carry between-shape variance, which cancels only in a paired same-shapes comparison. PER-SHAPE DATA FOR PAIRING: per_sample exists in evaluate_split's return dict with sid, gt_frac, bucket_frac, iou_by_thr and iou_std_by_thr, but out_json does NOT persist it (only aggregates plus the stratified bucket summary), so pairing is NOT computable from any existing artifact. Persisting per_sample is a small additive change and is the prerequisite for the lead's paired analysis.
- 2026-08-25 10:03 [agentic-train] TASK 1 DONE, TASK 2 RUNNING, TASK 3 PREMISE CORRECTED. (1) per_sample now persisted in eval_emissive.py's out_json: purely additive, no existing key changed, old jsons still readable. Deploy discipline followed: ddp-engineer's suite 44/44 before AND after, no eval job was mid-run (only training 249196, which loaded its code at start), ddp-engineer notified before the edit. Mirrored to the canonical checkout, uncommitted. The change carries a comment stating exactly what draw_std is, so the misreading that cost us a wrong conclusion in each direction cannot be repeated silently by the next reader. (2) Validation pair submitted at 5 draws each, both now writing per-shape rows: the epoch-8 warm-start reference itself (249198) and solar arm ep4 (249199). Re-running the reference through the same instrument is the lead's own requirement and it is also what makes a paired computation possible at all, since pairing needs both sides to have per-shape data. New tool emissive/eval/paired_compare.py computes the per-shape difference, its paired sd, se and t, alongside the unpaired and draw-noise-only figures so the three can be seen to disagree; it refuses with an explanation rather than a traceback on any json predating today's change. (3) THE FIR PREMISE IS WRONG AND THE DOC WILL NOT SAY IT. I read /home/xya120/scratch/lightgen/dit_agentic_xg.sbatch: it cds into another user's shared read-only tree, sources a venv from it, and runs python -u train.py directly. No copy, no mechanically derived surface, no pin; its own log prints '<no git access, read-only tree>'. Its 36 clean hours are evidence that a run stays trustworthy when nobody edits the tree it reads and its INPUTS are asserted, not evidence that pinning works. What it actually does well is assert inputs: emission_latents >= 72,421, pbr_latents >= 72,421, split file md5 == 159492b2b8d104ab63f3d13eeea394d0, all fatal on mismatch, plus a refusal to start on a node whose GPUs already hold >2 GB. That is the half we lack (we record code identity in TRAINER_AT_START.txt but assert nothing about inputs), and it is what the doc will gain.
- 2026-08-25 10:07 [agentic-train] I WAS WRONG ABOUT FIR AND THE LEAD WAS RIGHT; DOC WRITTEN AS INSTRUCTED. I read a different session's TRELLIS.2 lightning job (dit_agentic_xg.sbatch) and concluded our fir run does not pin. Our run is env_build/fir_train_agentic.sbatch and it DOES pin, verified by me directly: runs/emis_72kv2_cond_pw1b_agentic_fir/code/ holds MD5SUMS, data_toolkit, emissive, inference_full.py and trellis2; the sbatch copies that surface, writes an MD5SUMS manifest, then cds into the pin and torchruns $PIN/emissive/train/train_emissive.py, so it EXECUTES the pinned copy rather than merely creating it. Critically its surface INCLUDES data_toolkit, the exact directory my solar pin omitted and the direct cause of the 248521 outage. So fir is a corrected instance of my own pattern, not a counterexample to pinning. README_RUN_PINNING.md rewritten three-part per the lead: hand-enumerated surfaces fail (248521 kept as the worked example, with both its lessons); a complete surface plus an execution check works, with fir's script as the reference implementation and a grep recipe for deriving the surface instead of recalling it; and input-identity assertion adopted as a complementary discipline credited to the dit job's pattern without folding that job into our record. I TESTED THE RECIPE I PRESCRIBED and documented where it falls short, which matters because the shortfall is the same shape as the original bug: the grep correctly surfaces data_toolkit, inference_full and trellis2, but it CANNOT surface emissive/eval, because eval_emissive is a bare module name that resolves only via a sys.path insert the entry point performs at runtime. A reader following the recipe alone would build an incomplete pin and reproduce 248521. The doc now says the derivation is a floor rather than a proof, requires hand-adding any path the entry point puts on sys.path, and leaves the execution check as the thing that actually catches what both steps miss.
- 2026-08-25 10:10 [agentic-train] FIR EVAL CADENCE REPOINTED AT fir-runner's MIRROR; one writer, one mechanism. Retired my pull-and-lock over the ControlMaster channel: the fir watcher now reads outputs/emis_72kv2_cond_pw1b_agentic_fir/ as an ordinary local run dir, no ssh, no rsync, no copy lock. VERIFIED BEFORE SWITCHING rather than on assurance: the mirror's epoch_0006 is md5 9dad05a282e26a7c6f5a3d6b61a49d32, identical both to fir itself and to the fir_arm/ copy my ep2/ep4/ep6 evals actually read, so points scored before and after the switch remain comparable. fir_arm/ left untouched as the historical record of what those evals read; nothing new is written there. Two things I handled that the switch did not mention. The _fir_cont mirror does not exist yet, so the watcher tolerates its absence; and when it does appear its epoch numbering restarts at 1, which means it needs a continued-epoch offset. The watcher does NOT guess that offset: if _fir_cont holds checkpoints and no offset can be read from a HANDOFF marker it emits FIR_EVAL_ATTENTION and evaluates nothing from that dir, because a mislabelled point is worse than a missing one. Someone will need to tell me the epoch count the continuation resumed from. ALSO CORRECTED BOTH WATCHERS' STATISTICS to the fixed error model (se = draw_std/sqrt(n*draws), not draw_std/sqrt(draws)) and re-reported every point that had been announced under the wrong one. Restated: solar ep2 0.1444 is -11.6 se BELOW the reference; solar ep4 0.1516 is -11.8 se BELOW; fir ep2 0.1155 is -18.9 se BELOW; fir ep4 0.1191 is -16.9 se BELOW; fir ep6 0.1709 is -6.3 se BELOW. Five of five agentic points are below the epoch-8 warm start, and the verdict wording now reads BELOW rather than INDISTINGUISHABLE so the board's own record no longer carries the retracted reading. Validation pair for the paired analysis: 249198 (reference ep8) RUNNING, 249199 (solar ep4) queued.
- 2026-08-25 10:12 [agentic-train] FIR CONTINUATION MAPPING ARMED, and I derive the offset differently from how the lead described, deliberately. The lead correctly warned that fir-runner's HANDOFF counts GLOBAL epochs INCLUDING the 8 warm-start epochs (its continuation epoch_0001 = global 17) while our continued-epoch axis excludes the warm start (same checkpoint = continued 9). Rather than parse that global number and subtract 8, the watcher now derives the offset from the RESUMED-FROM CHECKPOINT FILENAME named in the marker: a continuation resuming from <first_run>/epoch_0008.ckpt has offset 8, so its epoch_0001 is continued 9 and epoch_0002 is continued 10. That is convention-independent, so it does not care whose axis wrote the marker, and it keeps working if the continuation ever resumes from some other epoch. Parsing the global count and subtracting a hardcoded 8 would be correct today and silently wrong the moment either assumption moves. The reason to care: an off-by-8 does not LOOK wrong in a results table, since continued 17 and continued 9 are both plausible labels, so this class of error survives review. Tested end to end against a realistic HANDOFF: offset 8 derived, continuation epoch 2 submitted as continued epoch 10 at 10 draws with tag val72kok_raw_real_agentic_fir_ep10_d10, continuation epoch 1 correctly skipped as odd, _ema files correctly excluded, and with the marker absent it still refuses and emits FIR_EVAL_ATTENTION rather than guessing. Every submission line now prints the offset and the mapping it produced, so a wrong offset shows up at submit time instead of in a table weeks later. STATUS: ddp4 249196 training, 9,056 optimizer steps per rank per epoch, 3 epochs to go. Validation pair 249198 (reference ep8) running, 249199 (solar ep4) queued behind it; those are the first two evals that will carry per-shape rows and unlock the paired analysis.
- 2026-08-25 10:49 [agentic-train] PAIRED RESULT IS IN AND IT OVERTURNS BOTH OF MY EARLIER READINGS AND THE PAIRING PREMISE. Validation pair completed: 249198 (reference ep8) and 249199 (solar ep4), 5 draws each, n=96 each, 96 per-shape rows each. Paired on the shared 96 shapes: arm 0.1580, reference 0.1913, difference -0.0333, sd of per-shape differences 0.4152, se 0.0424, t = -0.79 on 95 df, 95% CI -0.1164 to +0.0498. NOT RESOLVED, the interval contains zero. PAIRING DID NOT HELP AND CANNOT: it removed -4% of the standard error, i.e. it made it very slightly worse. The reason is visible in the data: per-shape outcomes are nearly independent between the two models, the same shape scoring near 0 under one and near 1 under the other, with 50 of 96 shapes worse, 45 better, 1 unchanged and individual swings spanning the full -1.000 to +1.000 range. There is no shared shape effect to subtract, so the difference inherits both models' variance instead of cancelling it. The lead's premise that most variance is BETWEEN shapes is wrong for this metric; the variance is stochastic WITHIN shape. BOTH MY EARLIER FRAMINGS WERE WRONG, in opposite directions, and the truth is that they answer different questions. draw_std/sqrt(draws) was simply the wrong formula. draw_std/sqrt(n*draws) is the right formula for a different question, namely how much the number moves if the same checkpoint is re-scored, and it is EMPIRICALLY CONFIRMED: re-running solar ep4 gave 0.1516 then 0.1580, a movement of 0.0064 against a predicted 0.0039, right order. But that quantity conditions on these exact 96 shapes and generalises to nothing, so it cannot support 'this arm is below the reference' as a claim about models. THE HONEST TWO-PART STATEMENT: conditional on these 96 shapes the arm is reliably 0.033 below the reference, five times the re-run noise; as a claim about shapes in general it is not resolved. THE BINDING CONSTRAINT IS SHAPES, NOT DRAWS, which means both the lead's 50-draw plan and my own 25-to-50-draw suggestion aim at the wrong axis. At shape-level sd 0.415, 96 shapes resolves only 0.083; resolving 0.05 needs about 265 shapes, resolving the observed 0.033 needs about 598, resolving 0.02 needs about 1,656. Draws shrink a term that is already five times smaller than the effect.
- 2026-08-25 10:51 [agentic-train] PROTOCOL PIVOT EXECUTED: SHAPES, NOT DRAWS. The 50-draw campaign is formally cancelled by the lead. THE ARITHMETIC THAT FORCED IT, from real per-shape rows rather than assumption: the sd of per-shape IoU differences between two checkpoints is 0.4152, so 96 shapes resolves only 0.083 at 95%, against an observed arm-vs-reference gap of 0.033. Resolving 0.05 needs about 265 shapes, 0.033 about 598, 0.02 about 1,656. Draws cannot substitute: they shrink the re-score term, empirically about 0.006 (solar ep4 re-scored 0.1516 then 0.1580 across two independent 5-draw runs), which is already five times smaller than the effect. GATE CHECK PASSED, so no fallback was needed: the team's shared 381-shape set is entirely inside val_72k (381 of 381), and all 381 carry the three core slat files AND cond.pth, so every one is usable under --cond real. FROZEN AS A NAMED SPLIT: dataset_direct/val381_team_v1, 381 symlinks, with val381_team_v1_MANIFEST.txt (sorted sid list) and val381_team_v1_README.txt recording provenance, the resolution arithmetic, and the verification command. sha256 of the manifest = 129cea7a7565b8f35328b2c2dad6b190b5135e8b8a8d5d4a9980ff04e177ae3d. The hash exists specifically so the condok96 drift trap cannot recur: that subset was scored, its parent val_72k pool then grew to 387, and a later val_72k eval silently scored a different sample. ONE THING THE INSTRUCTION DID NOT ANTICIPATE, now documented in the README: the tracking 96 is NOT a subset of the team 381. Ninety-five of the 96 are inside it; one is not, 12ad09c1001049d2921455cb6200307f. So the two sets are not nested and their numbers are not interchangeable, which is why tracking numbers keep the explicit 'conditional on the fixed 96' label. eval_pw1b_agentic.sbatch now takes an optional fourth argument, the split, defaulting to the tracking set so the running cadence is untouched. First two decisive evals submitted on the 381: reference ep8 re-scored (249222) and filtered ep4 (249223), 5 draws each, expected ~80 min each since runtime scales with shapes. fir ep8 and ddp4 ep8 follow as those checkpoints land.
- 2026-08-25 10:55 [agentic-train] VERDICT LANGUAGE CORRECTED ON THE BOARD: the BELOW verdicts are superseded by the two-part statement. ddp-engineer caught that my corrected formula was itself incomplete, and they are right; I verified it independently on my own per-shape rows rather than accepting it. draw_std/sqrt(n*draws) is the DRAW-NOISE component only and omits between-shape variance, which dominates. Measured on the two evals that carry per-shape data: solar ep4 has between-shape sd 0.2576, so the unpaired se against a fixed 0.198 is 0.0263, not the 0.0047 my formula gave, a 5.6x understatement; the reference ep8 has between-shape sd 0.3067, se 0.0313 against 0.0037, an 8.4x understatement. Restated properly, solar ep4 is -0.0400 from 0.198, which is -8.6 re-run se (conditional on these 96 shapes) but only -1.52 se as a general claim, NOT resolved. So my first correction overshot: I went from error bars about 10x too wide to error bars about 6-8x too narrow, and the truth is that the two numbers answer different questions. THE STANDING LANGUAGE IS NOW THE TWO-PART STATEMENT, and it is enforced in code rather than by discipline: both watchers print CONDITIONAL (delta in re-run se, explicitly conditional on the fixed set) and GENERAL (delta in unpaired between-shape se, with a resolved / NOT resolved verdict), and where a json predates per_sample the GENERAL half prints 'not computable from this json' instead of guessing. Every prior BELOW verdict on this board is superseded by that format. STRUCTURAL FIX WHILE DOING IT: the reporting logic now lives in autoeval/report_point.py instead of inline inside a shell double-quoted python -c. That embedding has broken twice, once when a backtick in a comment became command substitution and hung a tick, and once today when my splice left the quoting unbalanced and took BOTH watchers down until I rebuilt them. Both watchers verified rebuilt: syntax clean, zero backticks, a tick completes in 0.03s, and detection re-verified by clearing a mark and confirming the point re-reports in the new format. DEPLOY GATE HELD: eval 249219 is running, so the eval_emissive.py field additions are NOT deployed and wait for a clean window.
- 2026-08-25 10:57 [agentic-train] SECOND STATISTICAL ERROR FOUND BY ddp-engineer, VERIFIED, AND A FOURTH FOUND WHILE VERIFYING IT. Their finding: even for the conditional question, se_rerun must use the ROOT MEAN SQUARE of the per-shape draw stds, not their mean, because variances add and standard deviations do not. It matters here because s_i is wildly uneven: 21 of 96 shapes on one eval have s_i exactly 0 (they predict all-black every draw), so Jensen bites hard. Verified on my rows: solar ep4 mean_i(s_i) 0.1016 against rms 0.1809, se_rerun 0.00464 becomes 0.00826, 1.78x; reference ep8 0.0807 against 0.1614, 0.00368 becomes 0.00737, 2.00x. Their decomposition of my original error holds to two decimals: 1.78 x 3.18 = 5.67 against the 5.6x observed, and 2.00 x 4.25 = 8.49 against 8.4x. So my single wrong number was really TWO independent bugs, an estimator error and a wrong-question error, of similar size. MY OWN FOURTH ERROR, recorded because it is the most instructive: verifying their mean-vs-rms point I coded sqrt(SUM(s^2)/(n*K)) instead of sqrt(MEAN(s^2)/(n*K)) and reported an understatement of 17x, off by exactly sqrt(96). I committed a sum-versus-mean error inside the same expression while checking a mean-versus-rms error. That is four wrong statistics between two agents in one day on one quantity, which is why the fix is named fields plus RAW PER-DRAW VALUES rather than a warning comment: a stored derived number nobody can recheck is how a wrong one survives. RULING IN MY REMIT, now explicit rather than incidental: every inferential statement quotes iou_at_5, the FIXED 0.5 threshold, never best_thr. best_thr is selected to maximise IoU on the same data it is reported on, so it is optimistically biased and its se does not account for the selection; it stays a calibration diagnostic only. The standing verdicts already used iou_at_5 by luck rather than judgement, and these evals are threshold-flat (sensitivity 5e-4 to 7e-4), so adopting the convention costs nothing and closes the hole. ddof also settled: stored iou_std_by_thr stays ddof=0 for compatibility with every existing json, and the derived se fields apply sqrt(K/(K-1)) = 1.118, documented on both sides. Deploy still gated on eval 249219 (gt-mask-scout's sample56_latest); fields, raw draws and paired changes land together in one clean pass.
- 2026-08-25 10:59 [agentic-train] PAIRED RESULT WITH THE AGREED STATISTICS, and a fifth trap caught before it reached anyone. Solar ep4 against the re-scored epoch-8 reference, both 5 draws, 96 shared shapes, fixed 0.5 threshold. ALL SHAPES: difference -0.0333; CONDITIONAL on these fixed shapes se 0.0124, -2.7 se, differs; GENERAL se 0.0424, -0.79 se, NOT resolved, 95% CI -0.1164 to +0.0498. NONZERO-COVERAGE SUBSET (n=85): difference -0.0729; CONDITIONAL se 0.0099, -7.4 se, differs; GENERAL se 0.0431, -1.69 se, NOT resolved, CI -0.1574 to +0.0116. So the standing answer is unchanged in shape and now rests on correct statistics: on these fixed 96 shapes the arm is genuinely below the reference, and as a claim about shapes in general nothing is resolved, on either subset. THE FIFTH TRAP, caught by running the paired tool rather than trusting a single-run number. Splitting by subset made my single-run reporter compare the NONZERO mean (0.1196) against the constant 0.198, which is an ALL-SHAPES figure, and that mismatch printed 'resolved at -3.28 se'. The paired comparison against the reference's OWN nonzero mean (0.1925) says -1.69 se, NOT resolved. Comparing a subset mean to a whole-set constant manufactures a difference out of a population change. It is the same class as the error ddp-engineer flagged in item 3, one level along, and it would have produced a confident false headline. The reporter no longer compares any subset to the constant; it prints the subset mean bare and points at paired_compare.py, with the reason recorded in the code. TOOLS NOW MATCH THE AGREED SPEC: se_rerun uses the RMS form with the ddof correction, se_unpaired_full uses between-shape sd over the matching subset, the paired conditional se uses the per-shape draw stds rather than std_i(d_i), every figure carries the n of the subset it was computed over, and the threshold is fixed at 0.5 everywhere with best_thr excluded from inference. Note the conditional number moved with the RMS fix as ddp-engineer predicted: solar ep4 all-shapes reads -2.7 conditional se now, not the -8.6 the mean form gave.
- 2026-08-25 11:02 [agentic-train] RETROFIT COMPLETE: THE WHOLE CAMPAIGN'S INFERENTIAL BASIS RECOVERED FOR ZERO GPU. ddp-engineer's parser (8214836, fixed after my ddof ruling caught that it lacked the correction) reproduces my json-side numbers exactly on 249199: between_shape_std 0.2577 against my 0.2576, se_unpaired_full 0.02630 against 0.0263, se_rerun 0.00923 against 0.00923. Two independently written parsers, one on json and one on logs, agreeing to the last digit. Seven historical points retrofitted from surviving verbose logs, 96 rows each, no re-runs: filtered ep2 and ep4, solar ep2 and ep4, fir ep2, ep4 and ep6. PAIRED AGAINST THE RE-SCORED EPOCH-8 REFERENCE, general claim, all shapes, fixed 0.5 threshold: filtered ep2 -0.0396 (-1.31 se), filtered ep4 +0.0074 (+0.55 se), solar ep2 -0.0469 (-1.27 se), solar ep4 -0.0397 (-0.95 se), fir ep2 -0.0758 (-2.08 se), fir ep4 -0.0722 (-1.83 se), fir ep6 -0.0204 (-0.48 se). MULTIPLICITY, which I checked before letting a RESOLVED label stand: seven comparisons were made against the same reference, so the uncorrected 1.96 threshold expects 0.35 false hits by chance and we got exactly one. The Bonferroni threshold for m=7 is 2.69 se, and fir ep2 at 2.08 does NOT clear it. So after correction NO point in the campaign is resolvably different from the warm-start reference, and the single uncorrected hit is what chance alone predicts. THE TRAJECTORY IS STILL THE USEFUL READING and it is consistent across all three arms: every arm drops below the reference early and climbs back toward it, fir from -0.076 at ep2 to -0.020 by ep6, solar from -0.047 to -0.040, filtered from -0.040 to +0.007. That shape is visible in all three arms independently, which is worth more than any single point's significance, but it is a description of a trend and not a resolved difference. Practical consequence: the campaign as measured cannot distinguish any arm from the warm start on 96 shapes, which is exactly what the shape-count arithmetic predicted and why the 381-shape decisive evals are running.
- 2026-08-25 11:04 [agentic-train] THE CAMPAIGN'S REAL FINDING, and it explains why the aggregate IoU cannot separate the arms. ddp-engineer verified my paired numbers independently from logs (diff -0.0333, se_paired 0.0424, z -0.79, all exact) and found that PAIRING HURTS HERE: per-shape correlation between arm and reference is r = -0.076, so std(d) exceeds the independent combination and se_paired 0.0424 is worse than either arm's own unpaired se. Confirmed on my native rows: r -0.076 all shapes, -0.059 nonzero, pairing worse in both. The justification we had both been repeating, that between-shape variance dominates and pairing cancels it, is FALSE on this data. Verdict unchanged, reason retracted. BUT THEIR READ OF WHY IS WRONG, AND THE TRUTH IS MORE USEFUL. They inferred that shape difficulty does not transfer between checkpoints and both are producing near-arbitrary predictions. A full correlation matrix across all eight scored checkpoints says otherwise: the continuation checkpoints correlate STRONGLY with each other (solar ep2 to ep4 0.733, solar ep4 to fir ep6 0.887, fir ep4 to fir ep6 0.805, mean around 0.7-0.8), and the reference correlates with filtered ep4 at 0.915. There are two tight clusters, not noise. What is near zero is the correlation ACROSS the clusters (solar ep4 to reference -0.023, fir ep6 to reference -0.106). THE MECHANISM, from corr(gt_frac, IoU) per checkpoint: reference +0.912, filtered ep4 +0.975, filtered ep2 +0.335, solar ep2 +0.084, solar ep4 -0.097, fir ep2 +0.013, fir ep4 -0.183, fir ep6 -0.207. The reference and filtered ep4 succeed almost exactly in proportion to how much emission a shape actually has. The agentic continuations have LOST that coupling entirely. Split by coverage makes it concrete (note IoU is 1.0 on a zero-GT shape only if the model also predicts nothing): on the 11 zero-coverage shapes the reference scores 0.182 and filtered ep4 scores 0.036, while solar ep4 scores 0.382 and fir ep6 scores 0.482; on the 85 with coverage, filtered ep4 leads at 0.220 against fir ep6 0.131 and solar ep4 0.122. So the filtered arm COMMITTED to emission (matching the bucket migration the lead reported: zero-GT 0.364 to 0.036) while the agentic arms went the OPPOSITE way and became conservative, staying dark more often and losing accuracy where emission is real. Both trades land at similar aggregate IoU because the metric averages them against each other. THAT is why no arm separates on aggregate IoU and why it is the wrong instrument: it is not insensitive, it is measuring a trade-off along an axis the arms move in opposite directions on. The lead's instinct to make bucket profiles co-primary is right, and the coverage correlation is a single number that captures the same axis.
- 2026-08-25 11:07 [agentic-train] WEIGHTING CONFOUND RULED OUT ON TWO INDEPENDENT GROUNDS. (1) MAGNITUDE. The old code zeroed emissive_frac only on a transient meta.json miss, and the measured cold-scan transient-miss rate is 29 of 36,226 dirs, 0.080%, which scales to roughly 51 shapes across the filtered run's 63,129-dir cold scan. Under --emis_oversample a zeroed frac drops that sample's weight from (frac+0.1) to 0.1, so a mid-strength emitter at frac 0.2 is under-sampled 3x and a strong one at 0.5 by 6x, but at a 0.08% incidence the total emitter sampling mass lost is about 0.053%. The effect to be explained is corr(gt_frac, IoU) moving from +0.91 and +0.97 to -0.10 and -0.21, a wholesale reorganisation of which shapes the model succeeds on. A 0.05% perturbation of the sampling distribution cannot do that. (2) DIRECTION, which is the lead's own observation and is decisive on its own. The old code UNDER-samples real emitters, so the filtered arm should have seen relatively fewer emitters and become LESS committed to emission. It is the MOST committed arm we have (corr +0.975, and only 0.036 on zero-coverage shapes, meaning it predicts emission almost everywhere). The confound predicts the opposite sign of the observed effect. One honest limitation stated rather than buried: the 0.080% figure is measured on cond.pth and core-slat lookups, since the preflight probes those; the meta.json miss rate is not separately measured. It is the same NFS phenomenon on the same directories in the same scan, so it should be the same order, but it is an inference rather than a measurement. A COMPOSITION CHECK THAT DEEPENS THE PUZZLE RATHER THAN RESOLVING IT: comparing crosstab new_max (peak emission, 0-255) across the training sets, the agentic set is STRONGER than the set it was filtered from, mean 211.6 against 200.9, and the 26,836 shapes the QC dropped are WEAKER, mean 186.4. So the agentic QC removed the weaker emitters and kept the stronger ones, which makes the arms' conservativeness more counterintuitive, not less. Caveat: new_max is a PEAK, while the oversample weight uses meta.json emissive_frac, a COVERAGE fraction, so this measures a related but different quantity and does not settle the mechanism. I am not going to invent a mechanism to fit; the confound is dead, the finding stands as data-policy, and WHY cleaner-and-stronger emitter data yields a more conservative model is an open question worth its own investigation.
- 2026-08-25 11:10 [agentic-train] PRE-REGISTERED PROBE SUBMITTED (249250) and the field patch STAGED, NOT deployed. I was wrong to say the deploy window was clean: gt-mask-scout's 249219 finished, but my OWN decisive 381 evals 249222 and 249223 are running and pending, and they make the window dirty exactly as much as anyone else's job would. Holding. The patch is fully prepared against a scratch copy instead: raw per-draw IoUs stored (iou_draws_by_thr), named per-threshold se fields (between_shape_std, se_unpaired_full, se_rerun in the agreed RMS form with the sqrt(K/(K-1)) ddof correction, plus n_shapes and mean so no figure floats free of the subset it was computed over), draw_std retained unchanged and explicitly marked not an inference quantity, and a comment where the temptation lives warning against adding the two se's in quadrature. Syntax verified and the static guard checked: zero bare os.path.exists remain, all five file accesses go through probe_exists, so ddp-engineer's test_eval_path_has_no_bare_exists will not trip. It lands in one pass with the suite green before and after when the evals clear. THE PROBE ITSELF is the pre-registered coverage test. The lead stated the hypothesis BEFORE it ran, which is the right order and worth recording: the agentic QC did not merely drop weak emitters, it truncated the HIGH-COVERAGE tail (whole_object_rule dropped 4,061 shapes for high UV-texel coverage; the agent's keep-taste favoured small localized glows), so the agentic set should be BRIGHTER but SMALLER, and the model then learns emission is rare and small, never predicts broad emission, wins dark shapes, loses big emitters, and corr(gt_frac, IoU) decouples as observed. That is consistent with the crosstab peak result already in hand (agentic mean new_max 211.6 against 200.9). The discriminating statistics were named in advance: fraction of shapes with emissive_frac > 0.2 and > 0.5 in each training set. Job 249250 reads meta.json across all 36,226 agentic and 63,129 nocopy training dirs and reports both distributions. It stands or dies on those two numbers and I will report them whichever way they fall.
- 2026-08-25 20:32 [agentic-train] The warm-start 4-GPU job on venus-17 was cancelled (its epoch 1 and 2 checkpoints are kept). The freed GPUs now run a second copy of the fair from-scratch 37k fine-tune, same recipe as the fir run, saving every epoch. Two independent copies of the same run now race; the first to finish 8 epochs is the one we evaluate.
- 2026-08-25 20:55 [agentic-train] The long fair-arm run is training on venus-17: 32-epoch target, 7-day walltime, checkpoint every epoch. Measured GPU utilization in steady state is around 90 percent; the exact throughput number comes with the first epoch summary in a few hours.
outcome: 
