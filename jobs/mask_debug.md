title: Mask bake debug: why a saturated prediction renders streaked, not uniform
executor: gallery-runner
track: research
status: ongoing
started: 2026-08-11 14:07
updated: 2026-08-13 16:32
slurm: 
link: 
page: workspace/mask_debug
upstreams: ckpt8_eval
motivation: The owner proved by argument that a saturated prediction with correct UV application must render uniformly lit, yet the showcase pumpkin streaks while the fig7 lantern renders uniform under the same degenerate prediction; the streaks therefore indicate a real coverage or UV-application defect in the mask bake, and the earlier exoneration's ground-truth control was blind to misalignment.
log:
- 2026-08-11 14:07 [gallery-runner] Job started.
- 2026-08-11 14:07 [owner] We need a decicated page to debug this! illustrating / visualizing the data format of the predicted mask, and how it is converted to ... so illustrating / visualizing / debugging everything. In thoery, we should not have the bad 'pattern'
- 2026-08-11 14:18 [gallery-runner] gallery-runner: IN PROGRESS, found the likely real bug via a Blender shader-graph inspection (job 243241, complete). Jack-o-lantern's mesh has 8 separate UV layers (UVMap..UVMap.007), only layer 0 marked active; its Material.001 spans 18 SEPARATE mesh primitives sharing one material (confirmed via earlier mask-transfer log: prims=18/18). Both its Base Color and Emission texture nodes have Vector.is_linked=False, meaning Blender falls back to the implicit ACTIVE UV layer for ALL 18 primitives -- correct only for whichever primitives originally landed on layer 0, wrong for the rest. Pumpkin and desk-lamp materials are all prims=1/1 (single primitive each), so this specific mechanism cannot affect them -- consistent with the pumpkin's issue being genuinely different (real UV atlas padding, independently verified). Desk lamp materials additionally have zero texture nodes at all (is_linked=False on both sockets, no image), so they were never exposed to this class of bug either. Checking now whether jack-o-lantern's GT render shows the same artifact (would mean this hits both columns, not just predictions) and running a synthetic all-white control on jack-o-lantern specifically to confirm decisively. ETA ~10 min for both.
- 2026-08-11 14:35 [gallery-runner] gallery-runner: BOTH decisive checks complete. (1) GT render for the jack-o-lantern: uniformly near-black overall (GT frac=0.029), so it cannot test for the band -- same blindness as the earlier GT round-trip control, inconclusive by construction, not a finding either way. (2) Synthetic all-white texture through the IDENTICAL render code path: renders UNIFORMLY LIT, no dark band at all. This REFUTES my mid-investigation multi-UV-layer hypothesis (8 UV layers, Vector.is_linked=False on both Base Color and Emission texture nodes, Material.001 spanning 18 primitives) as the cause of the visible artifact -- that structural quirk is real but empirically does not produce a rendering defect; Blender's implicit active-UV-layer fallback handles all 18 primitives correctly in practice. Closed it out with a second independent-rasterization cross-check (same method as the pumpkin): parsed all 18 of Material.001's primitives directly from the glb JSON, rasterized their UV footprint independently of pred_mask_to_asset.py, got coverage=0.592, matching the tool's own logged cov=0.589 almost exactly, and the resulting image is visually identical to the real mask (same rectangle+circle+partial-rectangle chart layout). FINAL VERDICT for both shapes, now with the strongest possible evidence: the visible streaks/bands are real, independently-verified UV atlas coverage gaps (~40-50% unused canvas, common for these auto/artist-unwrapped assets), not a pipeline or render bug. Both a synthetic all-white positive control AND an independent write-side rasterization cross-check agree for both the pumpkin and the jack-o-lantern. Proceeding to build the debug page now with the complete story, including the multi-UV-layer false lead and how it was ruled out.
- 2026-08-11 14:44 [gallery-runner] gallery-runner: debug page built and published. https://aspis.cmpt.sfu.ca/projects/omages/yanxg/lightgen/workspace/mask_debug/index.html -- xgpage v3, 8 sections (verdict, npz visualization, texel mapping method, wireframe-over-mask overlay, synthetic all-white control side by side with the real streaky render for both shapes, shader-graph texCoord binding inspection incl. the ruled-out multi-primitive lead, desk-lamp + GT round-trip controls, provenance), 6 numbered figures, 14 images, all verified live (200s). Found and fixed a real bug in my own build script during QA: fig_row() takes (label, src) tuples, not dicts, and dict panels were silently rendering as literal 'label'/'img' text with broken src -- caught by grepping the live HTML for expected image filenames rather than trusting the build's exit code, not by eyeballing a screenshot. Caught before anyone else saw it. Ready for the owner's screenshot.
- 2026-08-11 14:49 [gallery-runner] gallery-runner: running the two tests team-lead specified. (1) UV bounds check: both shapes' TEXCOORD_0 accessor min/max (declared AND independently recomputed from raw data) are strictly within [0,1] for every primitive on both materials -- pumpkin's single primitive and all 18 of the lantern's. The tiled/wrapped-UV + REPEAT-sampler hypothesis is ruled out directly, not assumed. (2) Red-padding decisive test: built a debug emissive texture, white inside my rasterized chart footprint and saturated red in what I consider padding, routed through render_emissive.py's --pred_emission RGB-preserving code path (not --pred_masks, which would collapse red and white to the same binary 'lit' bucket and be uninformative, per the critique). Uses the identical UV-wiring code (donor = e_node or upstream_image(Base Color), same Vector-link-copy) as the mask path. Job 243289 submitted for both shapes, results pending.
- 2026-08-11 14:49 [gallery-runner] gallery-runner: red-padding render job 243289 confirmed running cleanly on solar (pumpkin box render in progress, sampling normally). Will report the actual images the moment it lands. Acknowledging the overlay-circularity point: correct, the wireframe overlay only validates the write side against itself and was never a test of what the renderer actually samples -- the red-padding test is the real answer and nothing is being claimed from the overlay alone.
- 2026-08-11 15:03 [gallery-runner] gallery-runner: RED-PADDING TEST RESULT, retracting the earlier no-bug verdict. Job 243289 complete, both shapes show red directly on the mesh surface, not just walls. Lantern (064e4156): red covers essentially the ENTIRE visible body surface (measured ~20% of full-frame pixels reddish including background, but the annotated overlay shows nearly the whole mesh silhouette is red, not just padding-adjacent edges) -- consistent with the multi-primitive/8-UV-layer mechanism I found and WRONGLY ruled out earlier (the all-white control could not see this, exactly per the critique: every possible sample location was white, so wrong-layer sampling was invisible to it). Pumpkin (48af42db): red appears specifically on the carved cutout-interior faces (the inner rim geometry of the eye/nose/mouth openings) -- a smaller, localized mismatch, mechanism not yet identified (this shape has only 1 primitive and 1 UV layer, so it is not the same multi-primitive cause; possibly related to the KHR_materials_pbrSpecularGlossiness/MIX-node wrapping found earlier). Both annotated images (reddish pixels highlighted green) staged at /project/3dlg-hcvc/omages/yanxg_scratch/mask_debug/redpad_{pumpkin,lantern}_annotated.png. Investigating the mechanism and a targeted fix now.
- 2026-08-11 15:04 [gallery-runner] gallery-runner: UV layer diagnostic job 243305 submitted for the lantern -- for each material slot and each of the 8 UV layers, dumps the UV bbox restricted to that material's polygons, to determine precisely which layer holds real per-primitive data vs degenerate/default data, and whether layer 0 (the render-active layer the shader actually reads) is degenerate for most of the 18 primitives. This will pin the exact mechanism before attempting a fix.
- 2026-08-11 15:18 [gallery-runner] gallery-runner: UV layer diagnostic complicates the picture, not confirms it. All 8 UV layers on the lantern's Material.001 report the IDENTICAL aggregate UV bbox (u=[0.0327,0.9257] v=[0.0316,0.9782], matching to 4 decimals across all 8), meaning layer 0 (the render-active layer) already spans the correct full union of all 18 primitives' real UV data, not a degenerate/wrong subset -- this does NOT match my 'wrong active layer' hypothesis. Reconsidering the redpad render result: the box-render setup uses 32 max_bounces/16 diffuse_bounces specifically because the box's ambient fill IS multi-bounce diffuse light (documented in RENDERING.md). My redpad texture interleaves red/white at fine spatial granularity (the coverage mosaic has many small charts), and neighboring 3D surface points are often neighboring in UV space too -- meaning legitimate multi-bounce color bleeding between adjacent red and white patches could wash the whole surface toward a blended warm hue WITHOUT any UV-sampling bug at all. The sampled 'reddish' pixel values I measured (e.g. 104,78,27) are a warm amber blend, not pure red (255,0,0), consistent with bleeding rather than direct red-texel sampling. Rerunning the identical red-padding test with diffuse_bounces=0 to isolate each surface point's own DIRECT emission color and remove the bleeding confound before drawing any conclusion -- will not report a verdict until this control is in.
- 2026-08-11 15:21 [owner] I also would like to see the rendering of the predicted mask on the sparse voxels. So we can know whether it is the prediction is bad, or it is because of the voxel mask to mesh mask/emissive conversion is buggy
- 2026-08-11 15:26 [gallery-runner] gallery-runner: found a strong candidate confound for the muted-color reddish result: scene.cycles.use_denoising=True is HARDCODED inside render_emissive.py's render() function, not exposed via CLI. Blender's AI denoiser would treat a deliberately adversarial fine-grained red/white boundary pattern (matching the fine UV-chart mosaic) as noise and blur it, which would explain why sampled 'reddish' pixels are a muted warm blend (e.g. 104,78,27) rather than pure red (255,0,0) or pure white (255,255,255), and why testing Closest vs Linear texture interpolation made no difference (denoise happens as a post-process, after texture sampling, regardless of interpolation mode). Patched my local test-only copy (render_emissive_closest.py, the SHARED file is untouched) to disable denoising, bumped samples to 512 for a clean signal without it, submitted job 243311. Voxel-space arbitration renders (owner's addition) also submitted in parallel, job 243312, reusing paper_v3/render_voxels.py unmodified via a small format-converter script (my pred_voxels npz -> its {cells,color,lit,meta} contract), grey body / orange-accent style, GT and predicted-threshold panels for both shapes.
- 2026-08-11 15:45 [gallery-runner] gallery-runner: FINAL VERDICT after ruling out three specific confounds (denoiser hardcoded on -- tested off, no change; texture interpolation Linear vs Closest -- tested both, no change; GI/diffuse-bounce color bleeding -- isolated with diffuse_bounces=0, no change). Retracting the earlier no-bug conclusion: red genuinely appears on both mesh surfaces, in a real, structured (not random-noise) pattern -- crisp partial splits within single cutout features on the pumpkin (e.g. the eye renders as a clean orange/white bisection, not a blur), and near-total coverage on the lantern. This confirms a genuine divergence between what the write-side rasterization considers covered and what the render actually displays, for these two fine-UV-mosaic assets specifically. HONEST LIMIT: the exact mechanism is not pinned despite ruling out three candidates and confirming triangle count is preserved through Blender's import (1,829,468 both raw glTF and post-import) and all 8 UV layers report the same aggregate bbox (so it is not simply 'wrong active layer' as first suspected). I am not claiming a tested code fix; I do not have one. Voxel-space arbitration renders (owner's addition) are clean and decisive on their own, independent of this open question: GT and predicted-threshold panels for both shapes, grey/orange style via paper_v3/render_voxels.py unmodified. Predicted panels are near-total solid orange on both shapes, matching pred_frac approx 1.0 measured directly in the npz -- this proves the blanket collapse is the model's own raw voxel-space output, before any mesh conversion touches it, fully independent of the open mesh-render question. Rebuilding the debug page now with the corrected verdict, the voxel section placed early per the owner's request, and an honest 'what remains open' section in place of a fix I cannot yet demonstrate.
- 2026-08-11 15:47 [gallery-runner] gallery-runner: page rewritten and republished with the corrected verdict. https://aspis.cmpt.sfu.ca/projects/omages/yanxg/lightgen/workspace/mask_debug/index.html -- 8 sections: verdict (retracted no-bug, confirmed divergence, mechanism open, callout marked warn), voxel-space arbitration placed early per the owner's request, the red-padding decisive test with images, write-side mapping (reframed, correctly scoped as write-side-only), a 'ruled out' section documenting all three confounds tested (denoiser/interpolation/GI bounces) plus the multi-UV-layer lead that looked right and was not, calibration controls, the npz reference, provenance. 18 images, 8 numbered figures, all verified live (200s). No fix section claiming a code change -- honestly states the mechanism was not pinned in the time available. Ready for the owner's screenshot.
- 2026-08-11 15:51 [gallery-runner] gallery-runner: implementing the UV-free fix per team-lead's direction. Wrote render_uvfree.py: assigns emission per FACE from a nearest-voxel lookup against the shape's own predicted/GT voxel field, reading face centroids directly from the raw glTF geometry (same proven centre/scale/to_voxel frame math as pred_mask_to_asset.py, no dependence on Blender's UV-layer handling at all). Values are written as face-corner (not point-domain) vertex colors so adjacent faces do not blend, multiplied by a per-material flat albedo constant read directly from the glTF JSON (baseColorFactor or the spec-gloss diffuseFactor), matching the mask x albedo formulation but with zero texture/UV involvement anywhere. Reuses render_emissive.py's box/camera/lighting machinery unmodified via import, same pattern render_voxels.py already uses. Includes a --white_floor option (off by default) for the separate brightness finding. Includes a hard assertion that Blender's post-import polygon count matches the raw glTF face count before writing any color, so a correspondence break fails loudly rather than silently scrambling. Validation job 243316 submitted: pumpkin with GT values (must reproduce ONLY the carved-cutout glow shape, nothing else) and with predictions (sanity: uniform, no chart pattern).
- 2026-08-11 15:56 [gallery-runner] gallery-runner: UV-free validation PASSES cleanly on the pumpkin. GT render shows ONLY the carved eye/mouth cutout glow, correct shape, zero chart artifacts -- confirms both the face-centroid nearest-voxel lookup and the Blender-polygon-order correspondence (5576==5576 asserted) are correct; a wrong-order assignment would have scrambled this into visible noise, not the coherent carved-face shape it produced. Predicted render shows the entire body uniformly, brightly lit, zero streaking, zero patchy darkness, zero chart pattern -- matches the voxel-space evidence (pred_frac~1.0) with a completely clean visual. Camera check: handoff_fig7's own build scripts never override azimuth/elevation/lens, so the paper row used the same defaults (38/17/52/1.06) render_uvfree.py already uses -- no adjustment needed for paper-row matching. Moving to the three paper shapes now: warhammer 4e383188516c46a58e96b1b7fc2f16a7, lightsaber ac6c953289bb4f56836c00830a7bb111, robot 34170054845344aeb199b842a3bf7e92, epoch-8 raw (best.ckpt), through render_uvfree.py, standard formulation plus a white-floor variant.
- 2026-08-11 15:58 [gallery-runner] gallery-runner: three-paper-shapes render (job 243317) in progress. Found the likely current-paper-figure source for the robot's SegviGen-Emission panel: handoff_fig7/images/emission_only_k0/pred_emis_1k_w1/34170054845344aeb199b842a3bf7e92_pred.png -- old 1k-w1 checkpoint, box mode. Confirms the owner's description exactly: a coherent glowing visor band plus scattered patchy small glints on the chest/hand/feet, not a clean region. Staging as the debug page's before/after comparison. Note: this is the old 1k checkpoint, not epoch-8, so it is the right 'what the paper currently shows' reference but not a same-checkpoint before/after -- the after panel will also be a checkpoint change, not purely a rendering-path change, and I will say so plainly in the caption rather than imply an apples-to-apples fix comparison.
- 2026-08-11 16:11 [gallery-runner] gallery-runner: caught a real bug in my own UV-free implementation before reporting a false darkness finding to the owner. All 6 prediction panels (warhammer/lightsaber/robot, standard+whitefloor) came back completely black. Direct check on the warhammer: its albedo is (1,1,1) pure white, not dark -- ruling out the mask-times-dark-albedo hypothesis for THIS shape -- but n_lit_faces=0 despite pred_frac=0.0061 measured at the voxel level. Root cause: I queried nearest-voxel-overall and checked if THAT was lit, instead of pred_mask_to_asset.py's actual convention (nearest LIT voxel specifically, gated at tol=2.0 voxel units). A face's single nearest occupied voxel is almost always another surface voxel, so a sparse lit region that does not happen to be literally the closest point gets silently dropped -- a much stricter, wrong test. Fixed to match the established tol-gated nearest-lit-voxel query exactly. Job 243318 re-running the pumpkin validation (confirms the fix does not regress the passing case) plus all three paper shapes under the corrected logic.
- 2026-08-11 16:12 [gallery-runner] gallery-runner: voxel-resolution caption added and republished per owner ask, measured directly from data not assumed. All four voxel panels sit on the same 512^3 grid: pumpkin 2,218,234 occupied cells (139,085 lit GT, all lit pred), lantern 1,126,845 occupied cells (56,567 lit GT, all lit pred). GT provenance stated explicitly: geometry is native 512^3, but the lit/unlit LABEL traces back to Dongchen's 256^3 emission bake upsampled by nearest neighbor, so GT and pred sit on the same grid but are not equally fine-grained underneath it. Live at the mask_debug URL, pages.json refreshed.
- 2026-08-11 16:15 [gallery-runner] gallery-runner: re-validation under the fixed query (job 243318) confirms team-lead's own finding exactly and rules out a rendering-side explanation. Pumpkin GT re-render still correct (9 lit faces on Pumpkin_Material, no regression from the fix). All three paper shapes still render zero lit faces even under the corrected tol-gated query -- warhammer 0/262153 relevant voxels lit, lightsaber 0/24624, robot literally 0/548047 (its max predicted value never exceeds 0.0018, nowhere near the 0.5 threshold). This is mathematically unavoidable: no query fix can light up faces near voxels that were never predicted positive in the first place. Confirms: render path correct, draw-0 masks genuinely empty. Building the honest fix now: patched dump_pred_fbv1.py locally (dump_pred_alldraws.py, shared file untouched) to save EVERY draw's full voxel array instead of only draw 0, since the aggregate summary.json already shows high variance (lightsaber ema mean=0.30 std=0.37, robot ema mean=0.20 std=0.39) meaning some UN-saved draws are far less degenerate than draw 0. Job 243319 running: all 5 draws, both raw and ema checkpoints, for the three paper shapes. Will report the per-draw table and render from whichever draw is the best honest choice per shape.
- 2026-08-11 16:17 [master] Measured the prediction files for the three paper shapes directly. The robot's prediction is truly empty in every variant, but the hammer and the sword do carry small emissive regions, so their black panels mean the renderer read the wrong input. The hammer's ground-truth panel is also black despite 22 thousand emissive voxels in the file, which points at a per-shape alignment bug in the voxel-to-face transfer. The runner is tracing the wiring; a separate runner is sampling fresh draws for the robot.
- 2026-08-11 16:18 [gallery-runner] gallery-runner: acting on the two-bug report. (1) Added the requested loud assertion to render_uvfree.py: it now prints source_voxel_lit vs total_lit_faces after every transfer and REFUSES to render (SystemExit) if the voxel mask is nonzero but zero faces transferred -- a silent zero can no longer happen again. (2) Working hypothesis on the hammer-GT-black report, testing directly rather than assuming: the GT panels team-lead measured for hammer/saber/robot came from job 243317, which ran BEFORE my nearest-overall-vs-nearest-lit-within-tol fix -- I had only re-run PUMPKIN's GT as a regression check plus the 3 shapes' PRED under the fixed query, never re-ran hammer/saber/robot's GT under the fix. Job 243320 submitted: re-renders GT for all three under the corrected query plus the new assertion, to test whether this is the SAME already-diagnosed bug rather than a second, shape-specific frame/orientation issue. Multi-draw dump (243319, all 5 draws x raw+ema) still running in parallel for the pred side.
- 2026-08-11 16:21 [master] Measured every saved draw for the three paper shapes. Usable, non-degenerate samples exist for all three: the sword and the robot have draws near their ground-truth emission fractions, and the hammer has one sparse but real draw. The first round had rendered only draw zero, which happened to be empty for every shape. Final panels are now being rendered from the chosen draws, with each panel labeled by checkpoint and draw.
- 2026-08-11 16:23 [gallery-runner] gallery-runner: root cause found and fixed. render_uvfree.py's local primitives_raw() reimplementation read raw local-space glTF vertex positions with NO node-transform applied, unlike pred_mask_to_asset.py's real primitives() which applies each node's world matrix via node_transforms(). Invisible for the pumpkin (single node, near-identity transform, which is why validation passed) but produced a grossly wrong face-centroid frame for the warhammer -- confirmed directly: face centroid bbox showed Y and Z swapped/offset against the actual voxel coords bbox under the broken code, and matched exactly once the real primitives() was substituted in. Fixed by importing and using pred_mask_to_asset.primitives() directly instead of the hand-copied reimplementation. Separately, per team-lead's own measurement (all draw-0 npzs near-empty) plus the multi-draw dump (243319): robot is genuinely bistable at both poles across all 10 measured draws (raw+ema), 4 empty and 1 near-blanket (ema draw1=0.9684) -- rendering that blanket draw per the specified fallback, labeled. Lightsaber has real non-degenerate middle draws; picked ema draw3 (frac=0.6177), closest to its own GT fraction (0.4842) among all 10 measured draws -- the most honest representative choice, not cherry-picked for looks. Warhammer stays genuinely near-empty across all 10 measured draws (max 0.0061), no non-degenerate or blanket draw exists anywhere in the checked set -- rendering the best-available (still near-empty) result, labeled honestly as near-empty rather than concealed. Job 243326 running: GT for all three under both fixes (frame + query), plus the three selected predictions in both formulations.
- 2026-08-11 16:24 [gallery-runner] gallery-runner: clarifying the guard-placement diagnosis before final render. The refusal was NOT checking per-material prematurely -- total_lit_faces is summed across ALL materials before the check fires, and the log correctly shows total_lit_faces=0 matching MAT 0's own count because the warhammer has exactly ONE material total (n_materials=1, confirmed earlier via direct glTF inspection), not a separate non-emissive handle material that went unprocessed. The real cause was the node-transform bug already found and fixed independently (primitives_raw() dropping node_transforms()). Proceeding with the final batch per the exact specification: consistent-EMA set (hammer ema draw0, saber ema draw3, robot ema_seed4 + ema_seed5 from the robot-draws workstream, standard+whitefloor), best-honest alternates (saber raw draw4, robot raw_seed2, standard only), GT for all three (already in flight via job 243326). Not duplicating the robot-draws workstream's compute, consuming its staged npz files directly.
- 2026-08-11 16:25 [gallery-runner] gallery-runner: final render batch submitted matching the exact specification, job 243330. Consistent-EMA set (hammer ema draw0, saber ema draw3, robot ema_seed4 + ema_seed5 both candidates), standard+whitefloor. Best-honest alternates (saber raw draw4, robot raw_seed2), standard only. Each output filename self-labels the exact weights/draw that produced it (e.g. 34170054845344aeb199b842a3bf7e92_ema-seed4_pred_uvfree.png). GT for all three (job 243326, both fixes: node-transform + query) still running in parallel.
- 2026-08-11 16:25 [gallery-runner] gallery-runner: correcting my own earlier framing per team-lead's fresh-seed measurement. The robot is NOT bistable at both poles -- that characterization only holds for the 10 re-dumped draws (which happened to land empty x8, near-blanket x1, one small mid value). The parallel fresh-seed run (robot_draws, job 243321) found real middle-mode draws: ema_seed4=0.289, ema_seed3=0.251, ema_seed5=0.034 against GT=0.080. Correct statement for the page: the middle mode exists but is rare, sampled by chance, not structurally absent -- a sampling-density finding, not a bimodal-collapse finding. My already-submitted job 243330 already renders both ema_seed4 and ema_seed5 as robot candidates, so no duplicate work needed there. Noting a parallel hammer_draws workstream (16 fresh seeds) is in flight for the warhammer too -- leaving hammer's final panel choice open per instruction, rendering the 0.0061 fallback meanwhile (already in job 243330, labeled near-empty). Both render jobs (243326 GT, 243330 predictions) still running.
- 2026-08-11 16:27 [gallery-runner] gallery-runner: hammer rescue confirmed and rendering. Job 243331 submitted: hammer raw_seed3 (frac=0.107, GT=0.085, the fresh-seed middle-mode draw team-lead found), standard+whitefloor, self-labeled filename. This means all three paper shapes were rescued by fresh-seed resampling -- the middle mode exists for every shape but is rare in a 5-draw sample, a sampling-density finding, not a structural collapse. Keeping the near-empty fallback render (already in 243330) so the page can show both: most draws land empty/degenerate, resampling finds a usable middle draw. Owner will pick between a strict consistent-EMA row (near-empty hammer) and a mixed row (hammer raw seed3, saber ema draw3, robot ema seed4 or seed5) -- both fully rendered and available. Jobs 243326 (GT) and 243330 (main prediction batch) still running.
- 2026-08-11 16:49 [master] The final panels are rendered and pass the eye check: the hammer and the sword match their ground truth closely, the robot glows in a plausible but less accurate pattern, and no panel shows the chart artifacts that started this investigation. A comparison grid went to the owner; the page is being updated with the final section and the fix summary.
- 2026-08-11 16:53 [gallery-runner] gallery-runner: final page assembly complete and republished. https://aspis.cmpt.sfu.ca/projects/omages/yanxg/lightgen/workspace/mask_debug/index.html -- new sections per team-lead's assembly order: resolution (GT-vs-prediction grid for all three paper shapes, artifact-free, the goal achievement), fix (UV-free path as adopted mitigation + both render bugs found with mechanism + seam-welding hypothesis as one-line future work), sampling-density (the full per-draw frac table, most draws empty/blanket, real middle mode rare but present for every shape). Verdict section updated to lead with the resolution. Provenance extended to cover render_uvfree.py, the multi-draw dump, and the two coordinated fresh-seed workstreams (robot_draws, hammer_draws). 25 images referenced, 10 numbered figures, all verified live with fresh cache-busted hashes, not just at build time. pages.json refreshed. Ready for the owner's review.
- 2026-08-11 16:54 [master] For your review: Open the page's paper-panels section and the grid I sent: do the three panels meet your bar for the paper figure, and which robot variant do you want (the fuller seed 4 or the sparser seed 5)?
- 2026-08-11 18:15 [owner] [owner] I am not satisfactory about the debug page. information is messy. And I don't really see a fix: for a fix, we shall have before & after. and explaining what has been done. that page does not have that, also, I feel the title is confusing, the text are too verbose and low information. I mean wtf is this: 'Red Is On the Mesh, and the Paper Panels Are Fixed Anyway'. If I were you, I would simply have a clear title. Then directly a teaser figure showing the before and after, then explainning why the bug, and how it is fixed in very concise words without detail-plaguing
- 2026-08-11 18:15 [master] Review complete; flag cleared.
- 2026-08-11 18:15 [master] Owner verdict: revise. The page gets rebuilt to the owner's prescription: a plain title, a before-and-after teaser figure first, then the cause and the fix in a few concise sentences, with the investigation detail demoted to the bottom. Same executor continues on this entry.
- 2026-08-11 18:23 [gallery-runner] Rebuilt the debug page around the owner's feedback: plain title, one before/after teaser figure directly under the dek (streaky pumpkin and black warhammer panel on the left, the same two shapes clean through the fixed render path on the right), then a short why-section and what-was-done table. Everything else (red test, write-side mapping, ruled-out leads, voxel views, the prediction file reference) stayed on the page but moved below and got compressed to findings only, with the process narrative cut. Also caught and fixed a mismatch while rebuilding: the pumpkin after-panel had been wired to the synthetic all-white control image instead of the pumpkin's actual prediction rendered through the fixed path; replaced it with the real render, which was already sitting on the cluster from an earlier job and just needed copying in. Published and verified live.
- 2026-08-11 18:24 [master] For your review: The page is rebuilt to your prescription (plain title, before/after teaser, concise why and what-was-done). Does this shape work, and do the three paper panels meet your bar (robot: seed 4 fuller vs seed 5 sparser)?
- 2026-08-11 18:36 [owner] [owner] The before and after should illustrated by a diagram: by just words it is hard to understand what is the difference in the processing. Also for the teaser we need more examples, definitely read and understand figure 5 in the paper, and include the three examples there. Plus, I want you to thoroughly read the current report, I feel I can't find its logical flow, meaning you should do a major rewrite by yourself, to make sure the page makes sense
- 2026-08-11 18:36 [master] Review complete; flag cleared.
- 2026-08-11 18:36 [master] Owner verdict: revise again, and this rewrite is the master's own work. Three orders: a diagram of the old versus new processing path, the teaser expanded to the paper's figure 5 examples, and a full rewrite for logical flow.
- 2026-08-11 18:47 [owner] [owner] Several further feedbacks: not mention with ckpt produced the prediction results. No explanation on how the 'picked' is done. The 'after' box rendering only shows white color mask, but our convention is albedo . mask if you check the paper you will know this
- 2026-08-11 18:53 [gallery-runner] Owner feedback round three: the paper's fig 5 comparison needs SegviGen-convention emission (mask times the object's own albedo texture), not a flat white mask like the earlier UV-free panels used. Dispatched a render job (243363) for hammer/lightsaber/robot GT plus their picked draws (hammer raw seed3, lightsaber EMA draw3, robot EMA seed4, plus EMA seed5 for contrast), using a new per-face renderer that samples each face's real base-color texture at its own UV centroid in raw glTF/Python, still with no UV atlas so no divergence risk, in the dark-room treatment that matches the paper's actual figure 5 setup. Team-lead is rewriting the page this round; I'm only producing the renders.
- 2026-08-11 18:59 [master] The master rewrote the page in full: a before-and-after teaser on the paper's figure-5 examples in the mask-times-albedo convention, a two-lane diagram of the old and new processing paths with the divergence marked, a section naming the checkpoint and the draw-selection rule, and the investigation material compressed into reference sections. The colored renders came from a dedicated job sampling each face's real base-color texture.
- 2026-08-11 18:59 [master] For your review: Rewritten to your prescription with the diagram, figure-5 teaser, checkpoint and pick-rule sections. Does the flow work now, and which robot draw for the paper: the fuller seed 4 or the sparser seed 5?
- 2026-08-11 19:16 [owner] [owner] per-face emission is a shitty idea, examine by yourself, the rendering quality is shit. Also, use box render please. Also, try to just allocate more compute for render to speed up , greatly speedup, no matter how much more compute is used in solar
- 2026-08-11 19:16 [master] Confirmed by direct inspection: the per-face path turns the robot's head texture into a triangle mosaic. New plan: back to the full-quality texture bake (mask times the albedo texture, per texel) rendered with the box setup, gated by a per-shape divergence probe since the streak bug is asset-dependent; render jobs fan out one job per panel with the owner's authorization to spend freely on Solar for rendering.
- 2026-08-11 19:23 [master] Integrity correction, found by the runner's own code re-read: the decisive probe images were never the probe. The render tool ignores the probe flag in box mode and fell back to the pumpkin's own ground-truth flame texture, whose white-core red-rim palette coincidentally matches the probe's. The page now marks that section invalidated, the divergence is downgraded from confirmed to leading hypothesis, and the probe is re-running under the fixed tool for all five shapes.
- 2026-08-11 19:28 [gallery-runner] Root-cause update on the red-padding evidence: found and fixed a real bug in my own debug tool. render_emissive_closest.py's box-mode branch never read the --pred_emission flag, so the earlier probe silently rendered the pumpkin's own native flame texture (an artist-painted white-hot/red-rim glow) instead of my white-inside/red-padding overlay; the crisp white/red split on the page was a coincidence, not evidence. Fixed the missing branch, re-ran pumpkin and lantern for real: both now show large, structural red regions (a broad equatorial band on the lantern, wide patches on the pumpkin) that are clearly not native texture, so the underlying streak-mechanism finding holds, it was just resting on invalid evidence before. Rebuilt the probe texture with an off-white value (250, not 255) and added a log line confirming which image actually reached each material's emission input, so this class of false positive cannot recur silently. Now running the full owner-directed plan: gating probes for the three paper shapes plus the seven full-resolution texture-bake box-render panels (GT and picked draws for hammer, lightsaber, robot), twelve solar jobs dispatched together (243379-243390).
- 2026-08-11 19:44 [master] The corrected probes settle it: the write-read divergence is real and structural on the paper shapes, with the robot's head half misread and the hammer's side panel misread. The box-render panels look excellent otherwise, with full texture detail on the hammer and robot. The sword exposes a second, unrelated gap: its blade material carries no texture, so the texture bake writes nothing and the blade stays dark. Two fixes are in progress: baking against the renderer's own imported UV coordinates so writer and reader agree by construction, and a flat-emission branch for textureless materials.
- 2026-08-11 19:55 [gallery-runner] UV-divergence mechanism found and confirmed with data, not hypothesis. For the same face, verified correct by an independent 3D-position check (raw glTF positions match Blender's imported positions to within 8 millionths of a unit once the standard glTF-to-Blender Y-up-to-Z-up axis convention is applied), the raw glTF UV and Blender's own imported UV differ by exactly one thing: Blender's V coordinate equals 1 minus the raw glTF V coordinate, with U completely unchanged, matching to three decimal places across every corner checked. This is the well known top-left-origin (glTF) versus bottom-left-origin (Blender) UV convention difference, which Blender's importer always applies and the write-side mask baker never did. Because the emissive texture atlas is not vertically symmetric, a full flip of the V axis does not just turn the image upside down, it scatters each UV island to an unrelated location relative to the others, which is exactly the scrambled streak pattern seen all along. The fix already in progress (rasterizing against Blender's own imported UV instead of the raw glTF UV) resolves this by construction, since it never reads the unflipped value at all. Proceeding now with the full re-render: the fixed rebake for all three paper shapes' ground truth and picked draws, plus two regression probes on hammer and robot rebuilt to use the same corrected UV, run together on solar.
- 2026-08-11 19:56 [master] The mechanism is found and verified: Blender flips the vertical texture coordinate on glTF import (the two formats count V from opposite ends), and the mask baker never applied the flip. Checked three ways: face correspondence is exact in 3D, no wraparound or wrong texture set, and the coordinates themselves show V mapping to one minus V on every face while U stays bit-identical. This one convention mismatch explains the streaks, the black sword blade, and every probe result. The rebake that reads coordinates from Blender itself is rendering all seven panels now, with two regression probes.
- 2026-08-11 19:57 [gallery-runner] Dispatched the full fix: nine solar jobs together, two regression probes (hammer 243399, robot 243400) rebuilding the red-padding texture from Blender's own imported UV the same way the real bake now does, and seven panel jobs (243401-243407) rebaking each mask against Blender's imported UV and rendering through the standard box setup (Filmic, exposure 1.5). Will report probe verdicts and panel pixel stats as they land.
- 2026-08-11 20:04 [master] The coordinate-flip fix is proven: the regression probes' misread fraction fell from two and five percent to zero on the hammer and robot. The sword's blade now lights for the first time through the texture path. One wiring defect remains: the hammer and robot panel jobs wrote empty emission (their probe jobs through the same path rendered fine), so those five panels are re-rendering after a targeted debug.
- 2026-08-11 20:13 [gallery-runner] Found the actual cause of the hammer and robot black panels, and it was not the flat-material branch or a probe-vs-panel divergence, it was a second, separate bug in my own bpy rebake script. bpyutil's own GLB loader does not just convert axis conventions on import, it also rescales every mesh so its longest bounding-box dimension is exactly 2.0, regardless of the asset's real authored size. The rebake was feeding those rescaled Blender positions into the voxel lookup using the frame computed from the raw, unscaled glTF geometry, so every query collapsed into a single wrong corner of voxel space and matched nothing. It was invisible on the saber only because that asset happens to already be authored near unit scale, so the rescale was close to a no-op there. Confirmed and fixed: after correcting for the 2x rescale, the query range now matches the true occupied-voxel range on both hammer and robot almost exactly, with real transferred counts (141,704 and 36,311 texels) where there were zero before. Also fixed the diagnostic gap: the per-material lit-texel count is now printed for every panel job, and the stats file no longer silently drops those numbers before saving. Resubmitted only the five affected panels (hammer GT and its picked draw, robot GT and its two picked draws); the saber pair and the two regression probes are untouched, as instructed.
- 2026-08-11 20:23 [master] The page now carries the complete, confirmed story: the coordinate-flip mechanism with its numbers, the before-and-after probe pair showing the misread fraction falling to zero, the fixed-bake diagram, and the final box-render panels at full texture quality. All render-path questions from this investigation are closed.
- 2026-08-11 20:23 [master] For your review: Final panels are in the grid I sent and on the page: box render, full texture, the sword's blade finally red, zero artifacts. Do they meet the paper bar, and which robot draw: seed 4 (fuller) or seed 5 (sparser)?
- 2026-08-11 20:33 [owner] [owner] this is not fixed: (desklamp_render_pred.png on the page)  also, I would like to see the prediction visualizations for all the sets we mentioned (dongchen's pick, ours pick), using the fixed bake, what do you say
- 2026-08-11 20:35 [owner] [owner] this 'open alternative' still seems weird, are you sure it is not bugged by the wrong uv bake thing? The pattern seems very random, if compared to (robot seed 4)
- 2026-08-11 20:54 [gallery-runner] Owner questioned whether robot EMA seed5's scattered pattern was a bake artifact, since seed4 by comparison looks coherent. Rendered both seeds directly in voxel space, grey and orange cubes only, no mesh, no UV, no bake anywhere in the path. Answer: seed5's voxel view shows the same thin, scattered speckle across nearly every region of the robot (head, torso, hands, legs) that the mesh panel shows, while seed4's voxel view is coherent, concentrated blobs (both eyes, both hands, both feet). The scatter is a property of that draw's own sampling, not a rendering artifact; the panel is honest.
- 2026-08-11 20:55 [master] The owner's challenge on the sparse robot draw is answered with a voxel-space arbitration: rendering the raw masks as cubes, with no mesh or texture coordinates anywhere in the path, shows the sparse draw is structureless dust at the source while the picked draw has solid coherent regions. The scatter is the model's own sampling, not a bake artifact, and the comparison also supports keeping the fuller draw as the pick.
- 2026-08-11 20:56 [master] The desk lamp's clean-control framing is withdrawn on the page: under the fixed bake its ground truth renders as before but its prediction shows no visible glow despite the bake reporting two lit materials, and the old structural-immunity explanation was wrong. Recorded as an open item; the gallery pipeline stays the priority tonight.
- 2026-08-11 22:38 [master] The fix tooling is now in the research fork and pushed: the corrected bake, the fixed probe renderer, the probe texture builder, the per-draw dump script, and the draw-selection helper, with the coordinate-flip mechanism documented in the commit message. Dongchen's clone picks it up on pull.
- 2026-08-12 08:18 [master] The board entry now links to its page. The link had been missing for two reasons found on the owner's question: this entry's page field still said page-pending from registration time, and the gallery entry used the page's short name while the inventory registers workspace pages under a workspace/ prefix. Both fixed by hand since the board tool has no verb for editing the page field; that verb is worth adding.
- 2026-08-12 17:13 [owner] [owner] yes, seed 4 is good for the robot
- 2026-08-12 17:13 [master] Review complete; flag cleared.
- 2026-08-12 17:13 [master] Owner verdict received: robot uses the averaged-weights seed 4 draw. The displayed row is final (hammer raw seed 3, sword averaged draw 3, robot averaged seed 4). The remaining step of the goal is recompositing the paper's comparison figure so its SegviGen column carries these panels.
- 2026-08-12 17:22 [gallery-runner] Figure 5 recomposite: traced qual_rendering.pdf composition - no compositor script exists in any repo (Producer metadata shows external app export, likely Keynote/PowerPoint on someone's Mac; git history is just 2 binary 'Update on Overleaf' pushes). Workaround found: the PDF embeds its 21 panels as separate image objects (not flattened), so a per-panel swap via PyMuPDF replace_image() works without the source file - validated end to end on a scratch copy, byte-identical elsewhere. BLOCKED on an exposure-parameter decision before staging a final: of the 3 ratified finals (box_hammer_rawseed3, box_saber_emadraw3, box_robot_emaseed4, all same box preset wall=0.80/exposure=1.5), saber matches its row's GT/TRELLIS.2 neighbors in brightness but hammer (mean lum 87 vs GT 63/TRELLIS.2 42) and robot (95 vs GT 30/TRELLIS.2 30, ~3x) come out substantially brighter - larger emitting surfaces bouncing more light off the same walls than saber's thin blade. Reported to team-lead with the quantified comparison, asking whether to accept as-is or re-render hammer/robot at lower wall albedo. No file staged yet pending that call.
- 2026-08-13 16:32 [gallery-runner] Figure 5 recomposite DELIVERED: staged /local-scratch2/xya120/studio/misc/lightgen/lightgen_overleaf/imgs/qual_rendering_v2.pdf (original imgs/qual_rendering.pdf untouched, md5 verified unmodified; no git commit). Note: the overleaf repo was resynced mid-task (commit 57143b3, figures/ split into imgs/+figs/); re-verified the panel mapping against the new path before touching anything, same file (identical CreationDate/pages, same xrefs).
outcome: 
