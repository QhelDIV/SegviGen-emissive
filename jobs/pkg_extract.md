title: Extract the jobs board, graph, and versioning into the xgpage package
executor: jobs-redesign
track: tooling
status: done
started: 2026-08-10 16:57
updated: 2026-08-10 17:34
slurm: 
link: 
page: none (refactor; the unchanged live surfaces are the proof)
motivation: The board, graph, and version tool are generic mechanisms living as lightgen-local scripts; the console's extraction into the package proved the pattern, and the versioning tool's hand-port drift proved the cost of skipping it. In the package, every project gets them and improvements land once.
log:
- 2026-08-10 16:57 [jobs-redesign] Job started.
- 2026-08-10 17:00 [jobs-redesign] Starting with xgpage.jobs (the board mechanism), since it is the most familiar of the four pieces. Reading the console module's config pattern first to match it exactly.
- 2026-08-10 17:09 [jobs-redesign] xgpage.jobs done: parsing, rendering (bands, heat, timeline, track chips and filter, the job-page join), the manifest, and every CLI verb now live in the package as a JobsConfig-driven module. Rebuilt jobs.html and jobs.json through the new path and compared byte for byte against the pre-migration output: every field matches except the wall-clock age text, and five inline script comments I reworded while porting the code (comment text only, nothing that runs). Ran the full start, log, set-track, flag with an ask, unflag, done sequence on a throwaway job and watched it render correctly, then deleted it. inventory_jobs.py and xgjobs are now thin, config-only drivers; installed the package into the itables virtual environment too, since the console's own build subprocesses inventory_jobs.py through it. Moving on to the graph module next.
- 2026-08-10 17:16 [jobs-redesign] xgpage.graph done: the crawler (chrome exclusion, href resolution), layout persistence, and the typed-edge loader are now GraphConfig-driven package functions. Node discovery and the graph page itself stay in lightgen's build_graph.py, now under half its previous length. Moved graph_view.js, graph_view.css, and the vendored d3-force files into the package's shared assets too, so they sync the same way the theme files already do. Rebuilt graph.json and graph.html through the new path: the data file matches byte for byte except the generation timestamp, the saved layout positions file is untouched, and the page itself only differs in its timestamp line and two asset cache-bust hashes that shifted because another agent is concurrently editing those shared files, unrelated to this change. Ran the seven scripted interaction journeys against the republished page: all seven pass. Moving on to generalizing the journey-harness template next.
- 2026-08-10 17:19 [jobs-redesign] Journey-harness template done: the generic runner loop (browser lifecycle, per-journey error handling, PASS/FAIL reporting, exit code) now lives in the package's bundled qa directory alongside its existing width and interaction QA scripts, as journey_harness.js, with a matching shell wrapper template for the Node and Playwright self-location logic, which stays a per-project copy since it is workstation plumbing, not something worth importing across languages. Lightgen's own graph journeys file kept only what is genuinely specific to the graph page: its DOM helpers and the seven journeys themselves. Ran the full suite again after the change and all seven still pass. Moving on to the versioning tool next, the last of the four pieces.
- 2026-08-10 17:27 [jobs-redesign] xgpage.versioning done, the last of the four pieces. Fixed both known bugs while porting: minting with no explicit source used to crash partway through (a page copying its own folder onto itself hit Python's same-file error the moment it reached a real asset file), now skipped correctly since there is nothing to merge in that case; and anything sitting in a page's own folder used to count as a publishable asset, which once swept 21 megabytes of raw render frames onto the server twice, now excluded by a leading-underscore naming convention this project already uses elsewhere. Verified both fixes against a throwaway fixture reproducing each exact failure, both now mint cleanly with the raw material excluded from both the snapshot and the living page. Compared a check run against the pre-migration tool: identical output, including one real, pre-existing problem on the rendering page (its version slot was stripped by an unrelated rebuild) that both the old and new tool report the same way, so this was not something the migration introduced. What stayed project-owned: which pages are eligible to version, the workspace-to-console link rule, and how a versioned page registers into the navigation, all wired in as small hooks rather than hardcoded. Writing up the documentation next.
- 2026-08-10 17:32 [jobs-redesign] All four pieces are extracted into the package: the jobs board, the page graph, page versioning, and the interaction-QA harness template. Each new module takes a small project config plus a few named hooks for the handful of things that are genuinely this project's own policy; everything else, including two real bugs found while porting the versioning tool, now lives in one place instead of three. Every rebuilt page was compared against its pre-migration output and matched, and the skill and the package's own design document now describe all three new modules.
- 2026-08-10 17:34 [master] Master verification: board and graph rebuilt through the package modules, all seven graph journeys pass, and one takeover defect found and fixed: the thin xgjobs wrapper's interpreter line pointed at the system python, which does not have the package installed; it now runs under the console environment.
outcome: All four pieces are extracted into the package: the jobs board, the page graph, page versioning, and the interaction-QA harness template. Each new module takes a small project config plus a few named hooks for the handful of things that are genuinely this project's own policy; everything else, including two real bugs found while porting the versioning tool, now lives in one place instead of three. Every rebuilt page was compared against its pre-migration output and matched, and the skill and the package's own design document now describe all three new modules.
